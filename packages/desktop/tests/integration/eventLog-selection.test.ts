// Phase 4 Wave 0 test scaffold — failing stubs.
// Implementation lands in Plan 04-04 (see `.planning/phases/04-event-api/04-VALIDATION.md`).
// Sources: D-21, I-11 in 04-CONTEXT.md, PLUG-07.

import { describe, it } from "vitest";

// Implementation components do not exist yet — Wave 3 creates them.
// import { render } from "@testing-library/react";
// import { EventLogDrawer } from "../../../src/mainview/components/EventLogDrawer";

describe("EventLog row selection (I-11)", () => {
	it.todo(
		"row click selects node; Canvas pans via existing focusedNodeId/selectedNodeId effect (I-11 resolution — no new camera-follow action needed)",
	);
});
