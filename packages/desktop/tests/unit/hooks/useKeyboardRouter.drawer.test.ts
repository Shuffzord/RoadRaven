// Phase 4 Wave 0 test scaffold — failing stubs.
// Implementation lands in Plan 04-04 (see `.planning/phases/04-event-api/04-VALIDATION.md`).
// Sources: D-18 in 04-CONTEXT.md, PLUG-07.

import { describe, it } from "vitest";

// Drawer shortcut does not exist yet — Wave 3 adds it to useKeyboardRouter.
// import { useKeyboardRouter } from "../../../src/mainview/hooks/useKeyboardRouter";

describe("useKeyboardRouter drawer shortcut (D-18)", () => {
	it.todo("Ctrl+Shift+L toggles drawer");
	it.todo("shortcut no-ops when input focused");
});
