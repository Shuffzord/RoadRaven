// Phase 4 Wave 0 test scaffold — failing stubs.
// Implementation lands in Plan 04-04 (see `.planning/phases/04-event-api/04-VALIDATION.md`).
// Sources: D-20 in 04-CONTEXT.md, PLUG-07.

import { describe, it } from "vitest";
// import { render } from "@testing-library/react";
// EventLogFilterBar component does not exist yet — Wave 3 creates it.
// import { EventLogFilterBar } from "../../../src/mainview/components/EventLogFilterBar";

describe("EventLogFilterBar (D-20)", () => {
	it.todo("source dropdown lists distinct sources from log");
	it.todo("Selected node only toggle disables when no node selected");
	it.todo("status filter lists statusConfig entries");
	it.todo("Clear button visible when any filter active");
});
