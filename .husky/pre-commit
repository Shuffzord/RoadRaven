bunx lint-staged
bunx @biomejs/biome check --diagnostic-level=error .
# Disabled for Phase 4 Wave 0 scaffold + per-task TDD cycles.
# Restore when Phase 4 ships. Full gate still runs in `bun run verify`
# (manually at wave boundaries) and in CI.
# cd packages/desktop && bunx tsc --noEmit
# bunx vitest run --reporter=dot

# TODO(fallow): enable once the branch is stable and the post-GSD dead-code
# cleanup has landed. Scoped to changed files so it's fast and actionable.
# Tracking: run `bunx fallow audit --changed-since=HEAD` locally until then.
# bunx fallow audit --changed-since=HEAD --fail-on-issues
