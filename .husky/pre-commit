#!/usr/bin/env sh
bunx lint-staged
bunx @biomejs/biome check --diagnostic-level=error .
cd packages/desktop && bunx tsc --noEmit
bunx vitest run --reporter=dot
cd "$(git rev-parse --show-toplevel)" && bunx fallow audit --changed-since=HEAD --fail-on-issues
