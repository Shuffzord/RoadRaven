---
phase: 05-packaging-distribution
verified: 2026-05-05T12:30:00Z
status: gaps_found
score: 5/6 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Tag-triggered CI produces installer artifacts with the filenames documented in the test scaffold, README install instructions, and release.yml comments"
    status: failed
    reason: "Electrobun's actual stable build emits `RoadRaven-Setup.exe` -> `stable-win-x64-RoadRaven-Setup.zip` (no `-stable` suffix), but `tests/release/installer-artifacts.test.ts`, `README.md` install steps, and `release.yml` comments all assume `stable-win-x64-RoadRaven-Setup-stable.zip` / `stable-linux-x64-RoadRavenSetup-stable.tar.gz`. Verified locally by running `bunx electrobun build --env=stable` — actual files written under `packages/desktop/artifacts/` are `stable-win-x64-RoadRaven-Setup.zip`, `stable-win-x64-update.json`, `stable-win-x64-RoadRaven.tar.zst`. The CI smoke step that runs this test inside `build-windows` and `build-linux` will FAIL on the first real `v*` tag push, which (a) blocks artifact upload via `if-no-files-found: error` -> `github-release` -> `publish-npm-*` -> `deploy-docs` cascade, and (b) sends end users to a download URL that 404s."
    artifacts:
      - path: "tests/release/installer-artifacts.test.ts"
        issue: "Regex `/^stable-win-x64-RoadRaven-Setup-stable\\.zip$/` matches no real artifact; same for Linux `RoadRavenSetup-stable.tar.gz`. Locally produced 3 failures, 11 passes against an existing artifacts/ directory."
      - path: "README.md"
        issue: "Lines 19, 31, 35 instruct users to download `*-Setup-stable.zip` / `RoadRavenSetup-stable.tar.gz` — neither name will exist on any future GitHub Release."
      - path: ".github/workflows/release.yml"
        issue: "Lines 40, 76 comment claims output is `*-Setup-stable.zip` / `RoadRavenSetup-stable.tar.gz`; the upload-artifact glob `stable-win-x64-*.zip` itself works, but the smoke step `bunx vitest run tests/release/installer-artifacts.test.ts` will fail."
    missing:
      - "Either: relax the test regexes to `/^stable-win-x64-RoadRaven-Setup\\.zip$/` (and Linux equivalent) OR investigate whether Electrobun has a flag to append the channel to the bundle name. Check both paths against `packages/desktop/build/canary-win-x64/RoadRaven-Setup-canary.exe` (which DOES carry the channel suffix on the inner exe but not the outer .zip) — confirm the canonical pattern."
      - "Update README.md Windows + Linux install sections to the actual filenames so the v1.0 user journey works on day one."
      - "Update release.yml comment headers to match real output."
human_verification:
  - test: "Push a `v0.0.2-test.1` (or first real `v1.0.0`) tag and observe the 6-job pipeline end-to-end on GitHub Actions"
    expected: "build-windows + build-linux complete; installer-artifacts smoke step passes against real Electrobun output; github-release attaches the .zip + .tar.gz + manifests; publish-npm-core + publish-npm-mcp succeed via OIDC (no NPM_TOKEN); deploy-docs renders https://shuffzord.github.io/RoadRaven/; auto-updater manifest at `https://github.com/Shuffzord/RoadRaven/releases/latest/download/stable-win-x64-update.json` returns 200 with valid JSON"
    why_human: "The release workflow has never run end-to-end against npmjs.com Trusted Publishers nor against GitHub Pages — RELEASE-OPS.md sections A-C are user-owned setup that cannot be exercised programmatically. Even if filename gap is closed, first real tag push is the only validation."
  - test: "Walk the manual a11y checklist in 05-A11Y-AUDIT.md against an installed CEF binary (Windows .exe and Linux .tar.gz)"
    expected: "All keyboard navigation rows PASS (14 items); focus indicators visible in dark/light/high-contrast; color is never the sole status indicator; PACK-03 'all file actions reachable from keyboard / toolbar' confirmed; sign-off line populated"
    why_human: "axe-core/playwright runs against vite preview port 4173 (production renderer bundle). CEF-specific rendering of --rv-* tokens, Radix ARIA injection, and focus rings is not exercised by automation per the documented R-04 caveat. Tab order is also not auditable by axe."
  - test: "User-acceptance signature on Known Issue M3 in 05-A11Y-AUDIT.md (subtree-collapse-on-mutation deferred to v1.1)"
    expected: "User signs the 'User acceptance' row in the M3 table accepting the workaround as v1-acceptable"
    why_human: "M3 is documented as 'awaiting user acceptance signature' in the audit doc — it is the gating signature for shipping with the known UX issue."
---

# Phase 5: Packaging & Distribution Verification Report

**Phase Goal:** Native installers build for Windows (`.exe` in `.zip`) and Linux (`.tar.gz`); `@roadraven/core` + `@roadraven/plugin-claude-code` published to npm at lockstep version with provenance; GitHub Pages docs site is live; accessibility audit pass + CONTRIBUTING.md + README polish complete the v1.0 surface.

**Verified:** 2026-05-05T12:30:00Z
**Status:** gaps_found (1 substantive gap + 3 human-verification items)
**Re-verification:** No — initial verification

---

## Verdict

**NEEDS WORK** — phase deliverables are 90% in place and structurally sound, but a real artifact-naming mismatch between the assertion suite (`installer-artifacts.test.ts`), README install instructions, release.yml comments, and the actual Electrobun output will cause the first real `v*` tag push to fail at the CI smoke step, even though the upload-artifact glob itself is correct. Closing the filename gap is a one-commit fix; first-tag end-to-end validation and human a11y/M3 sign-offs remain the residual risk.

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                                                                            | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | A tag-triggered CI workflow builds Windows `.exe` (in `.zip`) and Linux `.tar.gz` installers, attaches them to a GitHub Release, and uploads them with `if-no-files-found: error` so missing artifacts fail loud (PACK-01)                                                                                                                                       | ✓ VERIFIED | `.github/workflows/release.yml` lines 22–92: `build-windows` (windows-latest, `bunx electrobun build --env=stable`, upload glob `stable-win-x64-*.zip` + manifest + tar.zst with `if-no-files-found: error`) and `build-linux` (ubuntu-latest, identical pattern with `stable-linux-x64-*.tar.gz`). `github-release` job (lines 94–111) downloads both artifacts and uses `softprops/action-gh-release@v2` with `prerelease: false` + `generate_release_notes: true`. Trigger glob `v[0-9]+.[0-9]+.[0-9]+` (line 11) is stable-only.                                                                                                                                                                                                                                                                              |
| 2   | The auto-updater is wired to a stable-channel manifest URL hosted at a stable HTTPS path; canary deferred (PACK-02)                                                                                                                                                                                                                                              | ✓ VERIFIED | `packages/desktop/electrobun.config.ts:23-28` — `release.baseUrl: "https://github.com/Shuffzord/RoadRaven/releases/latest/download"` (Strategy A — `/releases/latest/download/` always resolves to the most recent non-prerelease Release). Updater poll path = `${baseUrl}/{channel}-{os}-{arch}-update.json`. Release workflow attaches `stable-win-x64-update.json` and `stable-linux-x64-update.json`. Tag pattern in `release.yml` rejects `v*-canary.*` and `v*-rc.*` (B-03 fix landed at HEAD `713e176`). `tests/release/manifest-url.test.ts` is the post-release URL probe (skip-gated locally; will run once `RR_TEST_MANIFEST_URL` is set).                                                                                                                                                              |
| 3   | PACK-03 platform invariants hold: `bundleCEF: true` on all three platforms, no ApplicationMenu dependency, SIGTERM flush handler registered                                                                                                                                                                                                                      | ✓ VERIFIED | `packages/desktop/electrobun.config.ts:5,19-21` — `const bundleCEF = process.env.ROADRAVEN_RENDERER !== "webkit"` applied to `mac`, `linux`, `win`. `grep -rn ApplicationMenu packages/desktop/src/` returns 0 matches. `packages/desktop/src/bun/index.ts:205-220` — `process.on("SIGTERM", ...)` flushes pending writes via `eventServerHandle.stop() + deleteSentinel() + flushPending()`; matching `process.on("SIGINT", ...)` covers Ctrl+C; `packages/desktop/src/bun/eventServerStandalone.ts:67` covers the standalone event-server entry.                                                                                                                                                                                                                                                                  |
| 4   | `@roadraven/core` is publishable with ESM-only `dist/index.js` + `.d.ts` (tsup), zod-only runtime dep enforced by an allowlist script wired into CI; `@roadraven/plugin-claude-code` flipped public; both packages get OIDC + provenance via `npm publish --access public --provenance`; `@roadraven/react` correctly excluded from the publish list (PACK-04) | ✓ VERIFIED | `packages/core/package.json` — `private: false`, `main`/`types`/`exports` all point at `dist/`, `files: [dist, README, LICENSE]`, `dependencies: { zod }`, `publishConfig: { access: public, provenance: true }`, build via `tsup` + `prepublishOnly: bun run build`. `packages/core/tsup.config.ts` exists (per 05-02 SUMMARY: format `[esm]`, dts, clean, external `[zod]`, target `node20`). `plugins/claude-code/package.json` — `private: false`, identical publishConfig, `bin: roadraven-mcp -> ./dist/index.js`. `scripts/check-core-deps.ts` allowlist `Set(["zod"])` returns `✓ packages/core has 1 dependencies, all on the allowlist.`. `release.yml:135-136, 157-158` — `npm publish --access public --provenance` (NOT `bunx npm publish`, NOT with `NPM_TOKEN`); zero `NPM_TOKEN` references in repo. `packages/react/package.json` — `private: true`, `peerDependencies: { react, react-dom }`, no `dependencies` block, intentionally still a stub. `scripts/bump-version.ts` lines 23-27 — three publishable targets only; `packages/react/package.json` explicitly excluded with comment "deferred to v1.1 (D-21)". CI invariants job (`ci.yml:50-64`) runs `requirements-edits.test.ts` 8/8 pass on every PR; lint job runs `check-core-deps.ts`. |
| 5   | `release.yml` deploys a Just-the-Docs Jekyll site to GitHub Pages on every tag push, gated on `if: startsWith(github.ref, 'refs/tags/v')` and sequenced after the GitHub Release; CONTRIBUTING.md + README polish + plugin-authoring guide all in place (PACK-05)                                                                                                | ✓ VERIFIED | `release.yml:161-191` — `deploy-docs` job: `if` tag-gate present (defense in depth on top of workflow trigger), `needs: [github-release]`, OIDC chain (`configure-pages@v5` → `jekyll-build-pages@v1` → `upload-pages-artifact@v3` → `deploy-pages@v4`), curl smoke against `https://shuffzord.github.io/RoadRaven/` after deploy. `docs/_config.yml` (Just-the-Docs `remote_theme`, `baseurl: /RoadRaven`), `docs/index.md` (landing page nav_order 1), `docs/plugin-authoring.md` (Event API guide with claude-code MCP wrapper as worked example, 6 references to `@roadraven/core`, 7 references to `v1.1`), front matter on 5 existing docs (architecture, dev-guide, rpc-and-ipc, design-system, logging). `CONTRIBUTING.md` covers local setup, `bun run verify`, fallow-as-local-tool note (D-22 invariant), Documentation subsection with explicit Ruby + Bundler + manual Gemfile snippet, Electrobun-not-Electron + bun-not-npm conventions, `.planning/` orchestrator-managed callout. `README.md` gains Install + Feature status + Documentation + Contributing sections. |
| 6   | The accessibility audit gate enforces zero critical/serious axe violations across the surfaces and themes that ship in v1.0 (PACK-06); the WAI-ARIA tree pattern fixes (chevron `tabIndex={-1}`, Shift+Tab guard) integrated cleanly                                                                                                                              | ✓ VERIFIED | `packages/desktop/tests/a11y/audit.spec.ts` — 10 axe surfaces: 1 welcome, 2 loaded, 3 side-panel, 4 ctx-menu dark, 5 dialog, 6 themes loop ['dark', 'light', 'high-contrast', 'contrast'] = 4 surfaces, 7 ctx-menu light (B-02 regression guard). The `contrast` theme inclusion at line 164 is the B-01 fix landing at HEAD (`7e2b2f4`). `auditPage` helper enforces `impact === 'critical' || impact === 'serious'` gate (D-20). `RoadmapNode.tsx:189` — chevron `tabIndex={-1}` (M1 fix, commit `d890ad0`); `useKeyboardRouter.ts:195` — Tab handler guarded by `&& !e.shiftKey` (M2 fix, commit `6e16777`). `Canvas.tsx:385` — `<div role="tree" aria-label="Roadmap tree">` wraps `<Tree>` (aria-required-parent fix, commit `db1934c`). `SidePanel.tsx:320` — `tabIndex={0}` on scrollable region (scrollable-region-focusable fix, commit `db1934c`). Tokens fixed in `index.css`: light theme `--rv-status-blocked: #c92020` (B-02 fix, line 197); contrast theme `--rv-text-primary: #d1d1d1` + `--rv-text-tertiary: #888888` (B-01 fix, lines 448-450). Auto-collapse regression revert (`3b73a6d`) merged via `95a17b7`. |

**Score:** 5/6 truths verified, 1 failed (filename mismatch).

---

## Required Artifacts

| Artifact                                                                                              | Expected                                                                                                                                                  | Status      | Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/release.yml`                                                                       | 6-job pipeline, OIDC publish, tag-stable-only trigger, `deploy-docs` last                                                                                  | ✓ VERIFIED  | 191 lines, 6 jobs in expected DAG, trigger anchored to `v[0-9]+.[0-9]+.[0-9]+`, OIDC chain present, no `NPM_TOKEN`, two `npm publish --access public --provenance` calls, `if-no-files-found: error` on both upload-artifact steps, deploy-docs `if: startsWith(github.ref, 'refs/tags/v')` defense-in-depth gate.                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `.github/workflows/ci.yml`                                                                            | lint + typecheck + test + invariants jobs; fallow stays commented (D-22)                                                                                  | ✓ VERIFIED  | 4 active jobs; lint job runs `bun run scripts/check-core-deps.ts` (D-23); invariants job runs `bunx vitest run tests/release/requirements-edits.test.ts`; fallow placeholder is commented out (D-22).                                                                                                                                                                                                                                                                                |
| `scripts/bump-version.ts`                                                                             | Validate-then-write across 3 publishable packages + electrobun.config.ts; reject if any cfg replacement no-ops (B-04 fix)                                  | ✓ VERIFIED  | Lines 12-69: parses all targets first, verifies `cfgRegex` matches before any write, aborts with actionable error otherwise. `packages/react/package.json` excluded with rationale comment. Locally tested: `bun scripts/bump-version.ts xyz` → exit 1 + "Invalid version".                                                                                                                                                                                                          |
| `scripts/check-core-deps.ts`                                                                          | CI-runnable allowlist enforcer                                                                                                                            | ✓ VERIFIED  | `Set(["zod"])`, exit 1 on any unknown dep, currently `✓ packages/core has 1 dependencies, all on the allowlist.`. **Note (W-04 from review, not yet addressed):** does NOT also check `peerDependencies` / `optionalDependencies` / `bundledDependencies`.                                                                                                                                                                                                                          |
| `packages/core/package.json`                                                                          | `private: false`, dist-pointing main/types/exports, files allowlist, publishConfig provenance                                                              | ✓ VERIFIED  | All seven fields present and correct. zod-only dep.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/core/tsup.config.ts`                                                                        | ESM + dts build with `external: [zod]`                                                                                                                    | ✓ VERIFIED  | (Per 05-02 SUMMARY VR; file content not re-read, but build pipeline confirmed: `dist/index.js` 1.4 kB ESM + `dist/index.d.ts` 5.2 kB; zod NOT bundled.)                                                                                                                                                                                                                                                                                                                              |
| `plugins/claude-code/package.json`                                                                    | `private: false`, files allowlist, `bin: roadraven-mcp`, OIDC publishConfig                                                                                | ✓ VERIFIED  | All fields present, `bin: roadraven-mcp -> ./dist/index.js`.                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `LICENSE` (root + per-package)                                                                        | MIT, byte-identical, in each tarball                                                                                                                      | ✓ VERIFIED  | Three byte-identical files at `LICENSE`, `packages/core/LICENSE`, `plugins/claude-code/LICENSE` per 05-02 SUMMARY (referenced by each package's `files` allowlist).                                                                                                                                                                                                                                                                                                                  |
| `packages/desktop/electrobun.config.ts`                                                               | `release.baseUrl` + bundleCEF on 3 platforms + identifier unchanged                                                                                       | ✓ VERIFIED  | `release.baseUrl` present (line 27); `bundleCEF` const + 3 platforms (line 5, 19-21); `identifier: "RoadRaven.electrobun.dev"` unchanged (R-05 invariant respected).                                                                                                                                                                                                                                                                                                                  |
| `tests/release/installer-artifacts.test.ts`                                                           | Skip-gated test; passes against real Electrobun output                                                                                                    | ✗ STUB-LIKE | Skip-gate present (`describe.skipIf(!hasArtifacts)`), but the regex assertions don't match what Electrobun actually emits (see Truth #1). Locally with `packages/desktop/artifacts/` populated by stable build: 3 fail / 1 pass / 0 skip — meaning the smoke step inside `build-windows` and `build-linux` will fail in CI on the first tag push.                                                                                                                                       |
| `tests/release/requirements-edits.test.ts`                                                            | 8 assertions including B-03 (release.yml tag glob)                                                                                                        | ✓ VERIFIED  | 8 tests passing locally. Includes B-03 fix assertion: regex requires anchored `v[0-9]+.[0-9]+.[0-9]+` and explicitly rejects `v*` wildcard and `v*-canary` patterns.                                                                                                                                                                                                                                                                                                                  |
| `tests/release/core-exports.test.ts`                                                                  | 2 assertions on @roadraven/core dist exports                                                                                                              | ✓ VERIFIED  | 2/2 pass after `bun run --cwd packages/core build`. `dist/index.js` and `dist/index.d.ts` both contain the documented schema names.                                                                                                                                                                                                                                                                                                                                                  |
| `tests/release/manifest-url.test.ts`                                                                  | Post-release URL probe                                                                                                                                    | ✓ VERIFIED  | Skip-gated on `RR_TEST_MANIFEST_URL` env var; intentionally skipped locally.                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/desktop/tests/a11y/audit.spec.ts`                                                           | 10 axe surfaces × selectors verified, zero critical/serious gate                                                                                          | ✓ VERIFIED  | 10 surfaces (welcome, loaded, side-panel, ctx-menu dark, dialog, dark, light, high-contrast, contrast, ctx-menu light). Selectors pre-verified in inline comments (Canvas `[role="application"]`, RoadmapNode `[data-source-id]`, side panel `aside[role="complementary"]`). Documented exclusions (`svg .rd3t-link`, `#root[aria-hidden='true']`) per Radix Portal pattern.                                                                                                          |
| `docs/_config.yml`, `docs/index.md`, `docs/plugin-authoring.md`                                       | Just-the-Docs site with Event API authoring guide                                                                                                         | ✓ VERIFIED  | All three present per 05-04 SUMMARY VR. Plugin authoring guide quotes real `plugins/claude-code/src/` snippets and references `event-api.json` sentinel + `IntegrationEvent` from `@roadraven/core`.                                                                                                                                                                                                                                                                                  |
| `CONTRIBUTING.md` (repo root)                                                                         | Local setup, tests, code style, conventions, fallow-as-local-tool note, Documentation Ruby+Gemfile snippet                                                | ✓ VERIFIED  | All sections present per 05-04 SUMMARY VR.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `README.md`                                                                                            | Install per platform (W-3 chmod +x for Linux, SmartScreen path for Windows), Feature status table, links to docs site + CONTRIBUTING.md                    | ⚠️ PARTIAL  | Sections all in place; SmartScreen + chmod +x callouts present; Feature status uses words ("available"/"deferred"/"planned") per CLAUDE.md no-emoji rule. **However:** download filenames (`stable-win-x64-RoadRaven-Setup-stable.zip` line 19, `stable-linux-x64-RoadRavenSetup-stable.tar.gz` line 31, line 35) do not match what Electrobun actually publishes — see Truth #1 gap.                                                                                                |
| `05-A11Y-AUDIT.md`                                                                                    | Audit method, findings table, manual checklist, sign-off                                                                                                  | ⚠️ PARTIAL  | Doc present and well-structured; automated audit signed off (`[x]`); 9 fixed blockers documented; PACK-03 invariants verified. **However:** "Manual checklist completed by" and most manual rows still `_PASS / FAIL_` placeholders; M3 user-acceptance signature row still unsigned. These are listed under human verification.                                                                                                                                                      |
| `RELEASE-OPS.md`                                                                                       | One-stop human checklist (npm Trusted Publishers, GH Pages source flip, dry-run, failure recovery)                                                         | ✓ VERIFIED  | Present at `.planning/phases/05-packaging-distribution/05-RELEASE-OPS.md`; sections A-C user-actionable per Plan 05-03.                                                                                                                                                                                                                                                                                                                                                                |

---

## Key Link Verification

| From                                                  | To                                                                | Via                                                                             | Status     | Details                                                                                                                                                       |
| ----------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tag push                                              | Windows + Linux installers attached to GH Release                  | `release.yml` 6-job DAG                                                          | ⚠️ PARTIAL | Workflow + permissions + DAG correct; OIDC publish steps correct; **smoke-test step inside build-{windows,linux} jobs will fail on real artifact filenames.** |
| Tag push                                              | npm publish (@roadraven/core + plugin-claude-code) with provenance | OIDC token from `setup-node@v4` + `npm publish --access public --provenance`     | ✓ WIRED    | Both `publish-npm-*` jobs use `setup-node@v4` (registry-url set), `permissions.id-token: write` inherited from workflow scope, no `NPM_TOKEN` reference.       |
| Tag push                                              | GH Pages docs site live                                            | `deploy-docs` job (configure-pages → jekyll-build-pages → upload → deploy-pages) | ✓ WIRED    | Job depends on `github-release` (sequential after release). `if: startsWith(github.ref, 'refs/tags/v')` defense-in-depth gate. Curl smoke at end.              |
| `packages/core/dist/index.js`                          | npm consumer `import { RoadmapSchemaSchema } from "@roadraven/core"` | tsup ESM build + `package.json.exports`                                          | ✓ WIRED    | `core-exports.test.ts` 2/2 pass; dynamic import resolves all 5 documented schemas.                                                                              |
| `electrobun.config.ts.release.baseUrl`                | `Updater.localInfo.channel()` poll URL                             | Electrobun runtime resolves `${baseUrl}/{channel}-{os}-{arch}-update.json`       | ✓ WIRED    | baseUrl present; manifests will land at correct URL after first tag push (per `manifest-url.test.ts` post-release probe).                                       |
| `useKeyboardRouter` Tab handler                        | "add sibling below" mutation                                       | `e.key === "Tab" && !e.shiftKey && focusedId` branch                            | ✓ WIRED    | M2 fix landed at line 195 — Shift+Tab no longer triggers add-sibling.                                                                                          |
| `RoadmapNode` chevron button                            | document tab cycle                                                 | `tabIndex={-1}` on the chevron                                                   | ✓ WIRED    | M1 fix at line 189 — chevron stays out of tab order; only `treeitem` cards are tab stops, satisfying the WAI-ARIA tree pattern.                                |
| `Canvas` `<Tree>` wrapper                              | `role="treeitem"` ancestor requirement                             | `<div role="tree" aria-label="Roadmap tree">` wrapper                            | ✓ WIRED    | Aria-required-parent fix at line 385.                                                                                                                          |

---

## Behavioral Spot-Checks

| Behavior                                                                              | Command                                                      | Result                                                                                                                                                                                                                                                                                                                                                            | Status |
| ------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `@roadraven/core` allowlist passes                                                    | `bun run scripts/check-core-deps.ts`                          | `✓ packages/core has 1 dependencies, all on the allowlist.` exit 0                                                                                                                                                                                                                                                                                              | ✓ PASS |
| Requirements-edits regression gate green (8 assertions including B-03 fix)            | `bunx vitest run tests/release/requirements-edits.test.ts`    | 8/8 passed in 181ms                                                                                                                                                                                                                                                                                                                                              | ✓ PASS |
| `@roadraven/core` dist exports load + parse                                           | `bunx vitest run tests/release/core-exports.test.ts`          | 2/2 passed in 207ms; all 5 schemas resolvable                                                                                                                                                                                                                                                                                                                    | ✓ PASS |
| Desktop typecheck clean                                                               | `bun run --cwd packages/desktop typecheck`                    | tsc --noEmit exits 0                                                                                                                                                                                                                                                                                                                                              | ✓ PASS |
| Installer-artifacts smoke matches real Electrobun output                              | `bunx vitest run tests/release/installer-artifacts.test.ts`   | **3 FAILED** of 4: real artifact `stable-win-x64-RoadRaven-Setup.zip`, expected `stable-win-x64-RoadRaven-Setup-stable.zip`. Linux artifacts not produced locally so additional Linux assertions fail too (env-only, but the Windows-side regex mismatch is not env-dependent). | ✗ FAIL |
| Manifest URL probe                                                                    | `bunx vitest run tests/release/manifest-url.test.ts`          | 1 skipped (RR_TEST_MANIFEST_URL not set — by design)                                                                                                                                                                                                                                                                                                            | ? SKIP |

---

## Requirements Coverage

| Requirement | Source Plan(s)              | Description                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ----------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PACK-01     | 05-01, 05-03                | Native installers: Windows `.exe` (in `.zip`) + Linux `.tar.gz`; deferrals documented                                    | ⚠️ PARTIAL | Workflow + bundleCEF correct; PROJECT.md/REQUIREMENTS.md/ROADMAP.md scope locked + regression-gated. **However:** documented filenames in test + README + workflow comments are wrong (Truth #1).                                                                                                                                                                                                                                                                       |
| PACK-02     | 05-01, 05-03                | Stable channel only; `release.baseUrl` resolves per-platform manifest; canary deferred                                   | ✓ SATISFIED | `electrobun.config.ts.release.baseUrl` correct; tag pattern stable-only (B-03 fix at HEAD); manifest-url.test.ts post-release probe present.                                                                                                                                                                                                                                                                                                                          |
| PACK-03     | 05-01, 05-05                | bundleCEF on 3 platforms; no ApplicationMenu; SIGTERM flush                                                              | ✓ SATISFIED | All three invariants verified by grep (4× bundleCEF, 0× ApplicationMenu, 1× `process.on("SIGTERM"`); also verified inline in 05-A11Y-AUDIT.md.                                                                                                                                                                                                                                                                                                                              |
| PACK-04     | 05-01, 05-02, 05-03         | npm packages published with allowlist + provenance + lockstep version; @roadraven/react deferred                          | ✓ SATISFIED | tsup build + dist exports + check-core-deps gate + OIDC publish + bump-version 3-target lockstep. @roadraven/react remains `private: true`.                                                                                                                                                                                                                                                                                                                          |
| PACK-05     | 05-04                       | README polish, GH Pages docs site, plugin authoring guide, CONTRIBUTING.md                                              | ⚠️ PARTIAL | All four artifacts present and substantive; deploy-docs job in place. **However:** README install-instruction filenames also affected by Truth #1.                                                                                                                                                                                                                                                                                                                  |
| PACK-06     | 05-05                       | A11y audit passes: keyboard nav, ARIA on context menu/dialog, color not sole status, focus indicators visible           | ⚠️ PARTIAL | Automated 10/10 axe surfaces GREEN; B-01/B-02 contrast fixes landed at HEAD; M1/M2 ARIA fixes landed; M3 deferred to v1.1 with documented workaround. **However:** human checklist sign-off + M3 user-acceptance signature still pending.                                                                                                                                                                                                                              |

---

## Blocker Fixes Verified

| Blocker | Fix landed at HEAD | Verification                                                                                                                                                                                                                                                  |
| ------- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B-01    | ✓ `7e2b2f4`         | `index.css:448-450` — contrast theme `--rv-text-primary: #d1d1d1` (was `#666666`), `--rv-text-tertiary: #888888` (was `#666666`); `audit.spec.ts:164` — for-loop now includes `"contrast"` so the fix is regression-gated.                                       |
| B-02    | ✓ `7e2b2f4`         | `index.css:197` — light theme `--rv-status-blocked: #c92020` (was `#ef4444`); `audit.spec.ts:195-208` — Test #7 "Context menu open in light theme" added as B-02 regression guard.                                                                              |
| B-03    | ✓ `713e176`         | `release.yml:11` — trigger anchored to `v[0-9]+.[0-9]+.[0-9]+`; `requirements-edits.test.ts:74-82` — regex assertion + canary/wildcard rejection assertions; runs in CI invariants job on every push.                                                          |
| B-04    | ✓ `4f7c60b`         | `bump-version.ts:34-58` — validate-then-write: ALL targets parsed first, cfg replacement verified before any write; `packages/react/package.json` removed from targets (D-21); aborts with actionable error if regex no-ops, preserving D-04 invariant.       |

All four code-review blockers genuinely landed in the working tree at HEAD `7e2b2f4`.

---

## Outstanding Warnings / Info from 05-REVIEW.md — Recommended Dispositions

These were left undisposed at the time of code-review filing. Phase-goal-relevance noted for each.

| Code  | Title                                                                                              | Phase-goal blocker? | Recommended Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | -------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W-01  | release.yml workflow-level permissions over-scoped                                                  | No                  | **Defer** — security hardening, not goal-blocker. Per-job least-privilege is well-known cleanup; can ship v1.0 with workflow-scope permissions and tighten in a follow-up. The two npm publish jobs DO need `id-token: write`; the only "scope leak" is `contents: write` on build-{windows,linux} which only call `actions/upload-artifact` (no Release write). Recommend tracking as a backlog cleanup ticket.                                                                                                                                                                                                                |
| W-02  | ci.yml has no explicit workflow `permissions:` block                                                | No                  | **Fix-now (cheap)** — one-line addition (`permissions: { contents: read }` at workflow scope). Single-line PR; reduces blast radius on older repos with default `write-all` token. Cost negligible; defer if explicitly wanting to bundle with W-01.                                                                                                                                                                                                                                                                                                                                                                          |
| W-03  | bump-version.ts regex replace uses no `g` flag                                                     | No                  | **Defer** — B-04 fix already added a `cfgUpdated === cfg` guard that aborts if no replacement occurs. The regex still matches the first `version: "..."` only, but with the existing config that's the correct field. Anchor to `app:` block as a follow-up if other `version:` fields appear. Track as backlog.                                                                                                                                                                                                                                                                                                              |
| W-04  | check-core-deps.ts ignores `peerDependencies` / `optionalDependencies` / `bundledDependencies`     | Borderline          | **Fix-now (cheap)** — extending the loop to those three fields is ~10 lines and closes a real evasion vector. The PACK-04 invariant is "zero desktop dependencies"; today the check only enforces it on `dependencies`. Recommend including in the same patch as the filename gap.                                                                                                                                                                                                                                                                                                                                              |
| W-05  | check-core-deps.ts hard-codes relative path `packages/core/package.json` from `process.cwd()`     | No                  | **Defer** — CI always invokes from repo root; local-dev convenience only. Track as backlog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| W-06  | installer-artifacts.test.ts skip semantics mask CI failures                                        | **Yes, related**    | **Fix-now (combine with Truth #1)** — the test should NOT skip when `CI=true` and the artifacts dir is missing; it should fail loud. This compounds Truth #1: today the test would fail in CI for the WRONG reason (regex mismatch); fixing W-06 means it will fail for either reason (regex mismatch OR missing dir), surfacing the real cause.                                                                                                                                                                                                                                                                                |
| W-07  | useKeyboardRouter Ctrl+C empty-focused branch leaks default browser copy                           | No                  | **Defer / accept** — gray-area UX nit, not a phase-5 goal. File as backlog with a one-line clarifying comment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| W-08  | useKeyboardRouter `isMenuOpen` / `isModalOpen` document-wide selectors over-broad                   | No                  | **Defer** — pre-existing pattern; tighten to Radix `data-state="open"` when next touching the file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| W-09  | Canvas.tsx onClick deselect uses identity comparison that breaks with portals/overlays              | No                  | **Defer** — UX nit, not goal-blocker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| I-01  | bump-version.ts console.log violates global "no console.log unless asked" rule                      | No                  | **Accept** — CLI scripts are the documented exception; add inline comment if any churn brings the file open again.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| I-02  | electrobun.config.ts hard-codes `version: "0.0.1"` that will drift                                  | No                  | **Accept** — RELEASE-OPS.md sections cover the bump-before-tag workflow. Documented; not a goal-blocker.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| I-03  | release.yml `softprops/action-gh-release@v2` not pinned to SHA                                       | No                  | **Defer** — supply-chain hardening; track as backlog. Dependabot can keep them updated once SHA-pinned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| I-04  | manifest-url.test.ts uses non-null assertion on URL                                                  | No                  | **Defer** — type-narrowing nit; trivial to clean up next time the file is touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| I-05  | useKeyboardRouter.ts enterChild uses bare parentArray[index] without bounds check                   | No                  | **Defer** — defensive coding; track as backlog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| I-06  | index.css `--rv-pulse` token defined but not mapped in `@theme`                                     | No                  | **Defer** — Tailwind utility convenience only; current `var(--rv-pulse)` consumers all work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

**Summary:** 0 of 9 warnings + 0 of 6 info items independently block the phase goal. W-04 and W-06 are the only "fix-now" candidates (cheap, security-relevant, and W-06 is directly entangled with the Truth #1 gap).

---

## Phase-Goal Regressions Found

| Area                  | Regression?         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Auto-collapse fix     | ✓ Integrated cleanly | `git log` shows `3b73a6d` "fix: revert subtree-collapse regression from aad416e (initialDepth + isNodeVisible)" landed via merge `95a17b7`. The revert removed `initialDepth={3}` from Canvas.tsx and the `isNodeVisible` callback from useKeyboardRouter.ts, restoring the pre-aad416e behavior. Camera-comfort improvements from aad416e (`animatePanTo` distance-aware duration, viewport-comfort-zone clamp, separation tweak) were KEPT — confirmed in commit message + diff stats. |
| WAI-ARIA tree pattern  | ✓ Integrated cleanly | M1 (chevron tabIndex={-1}) at `RoadmapNode.tsx:189` (commit `d890ad0`); M2 (Tab handler `&& !e.shiftKey` guard) at `useKeyboardRouter.ts:195` (commit `6e16777`). Both fixes survived the wider Phase 5 commit churn (verified at HEAD).                                                                                                                                                                                                                                          |
| Auto-updater contract  | ⚠️ Filename mismatch | Truth #1 — `release.baseUrl` is correct, but the manifest assumes a future release will publish files matching the documented filename pattern. The Updater itself uses `${baseUrl}/{channel}-{os}-{arch}-update.json` which IS what Electrobun emits (`stable-win-x64-update.json` matches), so the manifest URL probe will pass. The mismatch is on the installer .zip / .tar.gz user-download path, not the JSON manifest path. Auto-updater itself should still work.            |

---

## Anti-Patterns Found

| File                                               | Line     | Pattern                                                                                                                       | Severity   | Impact                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/release/installer-artifacts.test.ts`        | 21, 40   | Regex assertion `/^stable-win-x64-RoadRaven-Setup-stable\.zip$/` matches no real Electrobun output; will fail in CI on first tag | 🛑 Blocker | Breaks the entire goal: the smoke step in `build-{windows,linux}` jobs fails → no upload → `github-release` job has no artifacts → npm publish jobs may run but the GH Release is empty → users have nothing to download even if the docs site deploys. |
| `README.md`                                          | 19, 31, 35 | Documentation tells users to download files that won't exist on any release                                                  | 🛑 Blocker | Day-one user journey fails — even if the release pipeline somehow ships correctly-named artifacts, the README still misdirects.                                                                                                                                                                                                                                                              |
| `.github/workflows/release.yml`                      | 40, 76   | Comment headers describe wrong filenames; the `*.zip` / `*.tar.gz` upload globs themselves are correct                          | ⚠️ Warning | Cosmetic; misleads anyone reading the workflow source.                                                                                                                                                                                                                                                                                                                                          |
| `tests/release/installer-artifacts.test.ts`        | 14-17    | `describe.skipIf(!hasArtifacts)` silently skips when artifacts dir missing in CI (W-06 from review)                          | ⚠️ Warning | Compounds Truth #1: if the build step somehow exits 0 without producing artifacts, the test silently skips. Recommend Erroring when `CI=true`.                                                                                                                                                                                                                                                  |
| `scripts/check-core-deps.ts`                       | 19       | Only checks `dependencies`, not peer/optional/bundled (W-04)                                                                  | ⚠️ Warning | Evasion vector for the PACK-04 zero-desktop-deps invariant; fix is ~10 lines.                                                                                                                                                                                                                                                                                                                  |
| `packages/desktop/electrobun.config.ts`             | 11       | `app.version: "0.0.1"` will drift until first bump-version run (I-02)                                                          | ℹ️ Info    | Documented in RELEASE-OPS.md; pre-tag bump is a manual step.                                                                                                                                                                                                                                                                                                                                    |

---

## Deferrals — Out-of-Scope items honored

| Deferral                           | Honored? | Evidence                                                                                                                                                                                                                              |
| ---------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-05 macOS .dmg in v1              | ✓        | release.yml has no macos-latest job; PROJECT.md "v1.0 Deferrals" includes the row; requirements-edits.test.ts asserts the row exists.                                                                                                  |
| D-08 No canary channel in v1       | ✓        | Tag glob is stable-semver only; release.baseUrl resolves to `/releases/latest/download` (latest stable Release); canary tag pattern `v*-canary.*` reserved per RELEASE-OPS.md.                                                          |
| D-11 No code signing in v1          | ✓        | No signing certificates committed (verified: 0 .cer / .pfx / .p12 / .gpg files outside node_modules); Windows SmartScreen path documented in README; PROJECT.md "Code signing for v1.0" deferral row present.                          |
| D-14 Linux .deb deferred             | ✓        | release.yml builds .tar.gz only; installer-artifacts.test.ts asserts `expect(debFiles).toEqual([])`; PROJECT.md row present.                                                                                                          |
| D-21 @roadraven/react deferred       | ✓        | `packages/react/package.json` is `private: true`; bump-version.ts excludes the file with rationale comment; no react-package npm publish job exists in release.yml.                                                                  |
| D-22 fallow stays commented in CI    | ✓        | `ci.yml:67-86` — fallow job commented out; CONTRIBUTING.md notes it's a local-only informational tool.                                                                                                                              |
| D-23 core-deps allowlist enforced    | ✓        | `scripts/check-core-deps.ts` runs in lint job; allowlist `Set(["zod"])` only.                                                                                                                                                          |
| R-01 Linux .tar.gz, not .deb         | ✓        | release.yml + tests + README all use .tar.gz; .deb absence asserted in installer-artifacts.test.ts.                                                                                                                                  |

No v1.1-deferred work accidentally landed.

---

## Human Verification Required

### 1. End-to-end first-tag-push smoke

**Test:** Push a `v0.0.2-test.1` (or first real `v1.0.0`) tag and observe the 6-job pipeline end-to-end on GitHub Actions.

**Expected:**
- `build-windows` + `build-linux` complete (after Truth #1 filename gap is closed, the smoke step inside each job will pass).
- `github-release` attaches `.zip` + `.tar.gz` + manifests.
- `publish-npm-core` + `publish-npm-mcp` succeed via OIDC (no `NPM_TOKEN`); both packages appear on npmjs.com with provenance attestation.
- `deploy-docs` renders `https://shuffzord.github.io/RoadRaven/`.
- Auto-updater manifest at `https://github.com/Shuffzord/RoadRaven/releases/latest/download/stable-win-x64-update.json` returns 200 with valid JSON (set `RR_TEST_MANIFEST_URL` and run `manifest-url.test.ts` post-publish).

**Why human:** The release workflow has never run end-to-end against npmjs.com Trusted Publishers nor against GitHub Pages — RELEASE-OPS.md sections A-C are user-owned setup that cannot be exercised programmatically. Even if the filename gap is closed, first real tag push is the only validation.

### 2. Manual a11y checklist on installed CEF binary

**Test:** Walk the manual a11y checklist in `05-A11Y-AUDIT.md` against an installed CEF binary (Windows .exe and Linux .tar.gz).

**Expected:** All keyboard navigation rows PASS (14 items); focus indicators visible in dark/light/high-contrast; color is never the sole status indicator; PACK-03 "All file actions reachable from keyboard / toolbar" confirmed; sign-off line populated.

**Why human:** axe-core/playwright runs against `vite preview` port 4173 (production renderer bundle). CEF-specific rendering of `--rv-*` tokens, Radix ARIA injection, and focus rings is not exercised by automation per the documented R-04 caveat. Tab order is also not auditable by axe.

### 3. M3 (subtree-collapse-on-mutation) user acceptance

**Test:** User signs the "User acceptance" row in the M3 table in `05-A11Y-AUDIT.md` accepting the workaround as v1-acceptable.

**Expected:** Signature populated; final disposition row updated to PASS.

**Why human:** M3 is documented as "awaiting user acceptance signature" in the audit doc — it is the gating signature for shipping with the known UX issue.

---

## Gaps Summary

The phase is one substantive bug away from green. Every structural piece — workflow, OIDC publish, allowlist gates, ARIA fixes, contrast fixes, docs site, CONTRIBUTING.md — is in place and verified. The single blocker is a test/documentation/comment mismatch with what Electrobun actually emits as the stable `.zip` / `.tar.gz` filename. Fixing it touches three small files (test regex, README install steps, workflow comment headers) and converts the smoke step from "guaranteed CI fail on first tag" to "verifies real artifact names land."

Two warnings are recommended for the same patch (W-04 peer/optional dep allowlist, W-06 skip-semantics fail-loud-in-CI). The remaining 7 warnings + 6 info items can be backlog or deferred.

Three human-verification checkpoints remain (first-tag end-to-end, manual a11y checklist, M3 user acceptance).

### Punchlist before phase can close

1. **[BLOCKER]** Investigate Electrobun stable-channel `.zip` / `.tar.gz` filename pattern. Either:
   - **(A) Adjust the test regex** (and README + workflow comments) to `/^stable-win-x64-RoadRaven-Setup\.zip$/` and `/^stable-linux-x64-RoadRavenSetup\.tar\.gz$/` (drop the `-stable` suffix that doesn't appear in the bundle name), OR
   - **(B) Find an Electrobun config flag** that injects the channel into the bundle name to match the documented expectation.

   Whichever path is chosen, all four references must agree:
   - `tests/release/installer-artifacts.test.ts` (lines 21, 40)
   - `README.md` (lines 19, 31, 35, 36)
   - `.github/workflows/release.yml` (comment lines 40, 76)
   - Any other `RoadRaven-Setup-stable.zip` / `RoadRavenSetup-stable.tar.gz` strings in `.planning/` (informational only; keep current after the canonical path is decided).

2. **[Cheap fix-now]** Extend `scripts/check-core-deps.ts` to also check `peerDependencies`, `optionalDependencies`, and `bundledDependencies` (W-04).

3. **[Cheap fix-now]** Make `installer-artifacts.test.ts` fail loud (not skip) when `CI=true` and the artifacts dir is missing (W-06).

4. **[Cheap fix-now]** Add `permissions: { contents: read }` at workflow scope to `ci.yml` (W-02).

5. **[Human]** Push first real `v1.0.0` tag and observe the 6-job pipeline (after #1 lands).

6. **[Human]** Walk manual a11y checklist on the installed CEF binary; sign off in `05-A11Y-AUDIT.md`.

7. **[Human]** Sign M3 user-acceptance row in `05-A11Y-AUDIT.md`.

After 1-3 land and 5-7 complete, the phase can close.

---

_Verified: 2026-05-05T12:30:00Z_
_Verifier: Claude (gsd-verifier)_
