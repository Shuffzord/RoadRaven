---
phase: 06-agentic-roadmap-authoring
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - shared/types.ts
  - packages/desktop/src/bun/eventSchema.ts
  - plugins/claude-code/src/tools/schemas.ts
  - plugins/claude-code/tests/wsClient.request.test.ts
  - plugins/claude-code/tests/agent-tools.contract.test.ts
  - packages/desktop/tests/unit/bun/agentRequestHandler.test.ts
  - packages/desktop/tests/unit/store/roadmapStore.moveNode.test.ts
  - packages/desktop/tests/unit/ui/agentRpcHandler.test.ts
  - plugins/claude-code/tests/scaffold.e2e.test.ts
autonomous: true
requirements:
  - PLUG-AGENT-READ-01
  - PLUG-AGENT-READ-02
  - PLUG-AGENT-READ-03
  - PLUG-AGENT-READ-04
  - PLUG-AGENT-READ-05
  - PLUG-AGENT-READ-06
  - PLUG-AGENT-CREATE-01
  - PLUG-AGENT-CREATE-02
  - PLUG-AGENT-UPDATE-01
  - PLUG-AGENT-UPDATE-02
  - PLUG-AGENT-UPDATE-03
  - PLUG-AGENT-UPDATE-04
  - PLUG-AGENT-UPDATE-05
  - PLUG-AGENT-UPDATE-06
  - PLUG-AGENT-DELETE-01
  - PLUG-AGENT-FILE-01
  - PLUG-AGENT-FILE-02
  - PLUG-AGENT-FILE-03
  - PLUG-AGENT-TRANSPORT-01
  - PLUG-AGENT-TRANSPORT-02
  - PLUG-AGENT-SAFETY-01
  - PLUG-AGENT-SAFETY-02
  - PLUG-AGENT-SAFETY-03

must_haves:
  truths:
    - "REQUIREMENTS.md contains the full PLUG-AGENT-* requirements block (23 entries) with 'Phase 6' phase tag"
    - "shared/types.ts RoadmapRPCType.bun.requests contains an agentRequest entry whose response is a discriminated union {ok:true,data} | {ok:false,error,code,hint?,data?}"
    - "shared/types.ts AppSettings has an agentApi optional field with enabled?: boolean (D-15 kill-switch)"
    - "packages/desktop/src/bun/eventSchema.ts exports AgentRequestSchema and AgentRequest type, and IncomingFrameSchema is a 3-way union (Hello | AgentRequest | EventFrame) — D-15"
    - "plugins/claude-code/src/tools/schemas.ts exports a Zod input schema for every tool whose input shape is non-trivial (createNode, createRoadmap, findNodes, updateNodeMetadata, moveNode, deleteNode, saveFileAs, openFile, getNode)"
    - "Test files exist as failing/pending scaffolds for: wsClient.request correlation, agent-tools contract, agentRequestHandler dispatch+gates, roadmapStore.moveNode, agentRpcHandler drawer events, scaffold e2e — VALIDATION.md Wave 0 gaps closed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PLUG-AGENT-* requirements block (23 IDs, all mapped to Phase 6)"
      contains: "PLUG-AGENT-READ-01"
    - path: "shared/types.ts"
      provides: "agentRequest RPC contract entry + agentApi setting"
      contains: "agentRequest:"
    - path: "packages/desktop/src/bun/eventSchema.ts"
      provides: "AgentRequestSchema + 3-way IncomingFrameSchema union"
      contains: "AgentRequestSchema"
    - path: "plugins/claude-code/src/tools/schemas.ts"
      provides: "Zod input schemas for ~9 non-trivial tools"
      contains: "CreateNodeInputSchema"
    - path: "plugins/claude-code/tests/wsClient.request.test.ts"
      provides: "Failing/pending scaffold for request/response correlation"
    - path: "plugins/claude-code/tests/agent-tools.contract.test.ts"
      provides: "Failing/pending scaffold — one describe per net-new tool"
    - path: "packages/desktop/tests/unit/bun/agentRequestHandler.test.ts"
      provides: "Failing/pending scaffold for Bun dispatcher (gate checks, error taxonomy, kill-switch)"
    - path: "packages/desktop/tests/unit/store/roadmapStore.moveNode.test.ts"
      provides: "Failing/pending scaffold for moveNode store action (cycle detection guard for agent path)"
    - path: "packages/desktop/tests/unit/ui/agentRpcHandler.test.ts"
      provides: "Failing/pending scaffold for renderer dispatcher (drawer events, PATCH semantics)"
    - path: "plugins/claude-code/tests/scaffold.e2e.test.ts"
      provides: "Failing/pending scaffold for the named end-to-end story"
  key_links:
    - from: "shared/types.ts"
      to: "packages/desktop/src/mainview/rpc/agentRpcHandler.ts (later plan)"
      via: "RoadmapRPCType.bun.requests.agentRequest"
      pattern: "agentRequest:"
    - from: "packages/desktop/src/bun/eventSchema.ts"
      to: "packages/desktop/src/bun/eventServer.ts (Plan 02)"
      via: "AgentRequestSchema in IncomingFrameSchema union"
      pattern: "z.union\\(.*AgentRequestSchema"
    - from: "plugins/claude-code/src/tools/schemas.ts"
      to: "plugins/claude-code/src/server.ts (Plan 04)"
      via: "Zod schema imports for inputSchema fields"
      pattern: "CreateNodeInputSchema"

user_setup: []
---

<objective>
Wave 0 foundation: lock down every contract, type, and test scaffold the rest of the phase will fill in. Adds the PLUG-AGENT-* requirements block, the agentRequest RPC type, the AgentRequestSchema Zod parser, the kill-switch setting hook, all plugin-side tool input schemas, and 6 failing/pending test files (one per Wave 0 row in 06-VALIDATION.md). After this plan: every later wave can write code without having to also invent the contract; every test file exists for executors to slot tests into.

Purpose: prevent Wave 1+ executors from re-inventing types, settings shape, or test layout. The Nyquist rule (every <verify> automated) requires the test files to exist before feature work — Plan 02 onward lights up these scaffolds.

Output: REQUIREMENTS.md updated, contract types added, Zod input schema module created, 6 test scaffold files committed (RED state — most cases assert the implementation exists; some are pending placeholders).
</objective>

<execution_context>
@C:/Work/RoadRaven/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/RoadRaven/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-agentic-roadmap-authoring/06-CONTEXT.md
@.planning/phases/06-agentic-roadmap-authoring/06-RESEARCH.md
@.planning/phases/06-agentic-roadmap-authoring/06-VALIDATION.md
@.planning/phases/06-agentic-roadmap-authoring/06-PATTERNS.md
@CLAUDE.md
@.claude/PRINCIPLES.md
@shared/types.ts
@packages/desktop/src/bun/eventSchema.ts
@packages/core/src/schema.ts
@plugins/claude-code/src/server.ts
@packages/desktop/tests/unit/bun/eventSchema.test.ts

<interfaces>
<!-- Existing types Wave 0 builds on. Executor uses these directly — no exploration. -->

From `shared/types.ts` (current — line 29-37):
```typescript
export interface AppSettings {
  theme?: ThemePreference;
  recentFiles?: string[];
  fileSettings?: Record<string, { layout?: "TB" | "LR" }>;
  eventApi?: {
    /** User-specified WebSocket port override. When set, no auto-fallback on EADDRINUSE. */
    port?: number;
  };
}
```

From `shared/types.ts` (current `bun.requests` block — copy `getEventApiState` shape as the structural model for `agentRequest`).

From `packages/core/src/schema.ts` exports (verified): `RoadmapNodeSchema`, `RoadmapSchemaSchema`, `StatusConfigSchema`, `TypeConfigSchema`. These are imported by `tools/schemas.ts`.

From `packages/desktop/src/bun/eventSchema.ts` (current — line 29):
```typescript
const IncomingFrameSchema = z.union([HelloFrameSchema, EventFrameSchema]);
```
This becomes a 3-way union after Task 3 below.

Existing wsClient test pattern (from `plugins/claude-code/tests/wsClient.test.ts`): uses MockWebSocket class with `instances`, `addEventListener`, `emit("open"|"message", ...)`, `send.mock.calls.at(-1)`. Reuse for `request.test.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PLUG-AGENT-* requirements block to REQUIREMENTS.md and update Phase 6 Plans count placeholder in ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current — note "## v1 Requirements" → ### subheadings: Scaffold, Theme System, Read-Only Viewer, Editor, Event API (PLUG), Packaging — and the Traceability table at the bottom)
    - .planning/phases/06-agentic-roadmap-authoring/06-RESEARCH.md §"Phase Requirements" (the proposed 23-entry catalog)
    - .planning/phases/06-agentic-roadmap-authoring/06-CONTEXT.md (D-01 through D-18 — the source of authority for every PLUG-AGENT-* ID)
  </read_first>
  <action>
    Append a new requirements section AFTER the existing `### Event API (PLUG)` block and BEFORE `### Packaging (PACK)` in `.planning/REQUIREMENTS.md`. Use this exact heading and 23 IDs (matching RESEARCH.md §"Phase Requirements" + adding the Phase-4 carry-forward as PLUG-AGENT-UPDATE-06):

```
### Agentic Roadmap Authoring (PLUG-AGENT)

> **Scope note:** Phase 6 — bidirectional MCP API extending PLUG-08 (Phase 4) into a full read/create/update/delete/file-lifecycle tool surface. Agent reaches the running app via the existing sentinel-discovered WebSocket; no second transport, no auth, no plugin-host activation. Builds on Phase 4 PLUG-09 parsed-but-inert `plugin`/`subscribe` fields without activating them.

- [ ] **PLUG-AGENT-READ-01**: `getRoadmap` MCP tool returns the full schema tree with live-overlay-merged statuses (D-07); fails with `app_not_running` (D-05) or `no_file_loaded` (D-06)
- [ ] **PLUG-AGENT-READ-02**: `getNode` MCP tool returns a node + immediate `parentId` + root-to-node `ancestorIds[]`; status reflects D-07 merge; `node_not_found` on missing id
- [ ] **PLUG-AGENT-READ-03**: `findNodes` MCP tool accepts a structured filter `{titleContains?, status?, type?, metaKey?, metaValue?, parentId?}` AND-combined; case-insensitive `titleContains`; no DSL (D-03)
- [ ] **PLUG-AGENT-READ-04**: `getStatusConfig` MCP tool returns the loaded schema's `statusConfig[]`
- [ ] **PLUG-AGENT-READ-05**: `getTypeConfig` MCP tool returns the loaded schema's `typeConfig[]`
- [ ] **PLUG-AGENT-READ-06**: `getOpenFile` MCP tool returns `{filePath, isUntitled, title, nodeCount}`; only requires app running (D-05) — does NOT require a file loaded
- [ ] **PLUG-AGENT-CREATE-01**: `createNode` MCP tool adds a child under `parentId`; optional `type/status/notes/metadata`; returns the new UUID; emits drawer audit event (D-09)
- [ ] **PLUG-AGENT-CREATE-02**: `createRoadmap` MCP tool initialises an in-memory untitled schema via the existing `newUntitledSchema` action (mirrors File > New); accepts optional `title/statusConfig/typeConfig`
- [ ] **PLUG-AGENT-UPDATE-01**: `renameNode` MCP tool changes a node's title via existing `roadmapStore.renameNode`; emits drawer event
- [ ] **PLUG-AGENT-UPDATE-02**: `updateNodeType` MCP tool changes a node's type via existing `roadmapStore.updateNodeType`; emits drawer event
- [ ] **PLUG-AGENT-UPDATE-03**: `updateNodeNotes` MCP tool replaces a node's notes via existing `roadmapStore.updateNodeNotes`; emits drawer event
- [ ] **PLUG-AGENT-UPDATE-04**: `updateNodeMetadata` MCP tool applies a shallow PATCH to `node.metadata` — null value deletes a key, unlisted keys are preserved (D-04); emits drawer event
- [ ] **PLUG-AGENT-UPDATE-05**: `moveNode` MCP tool re-parents a node to `newParentId` at optional `position`; rejects with `move_would_create_cycle` (target is descendant) or `cross_ref_boundary` (target in a different `$ref` file, EDIT-16); emits drawer event
- [ ] **PLUG-AGENT-UPDATE-06**: `updateNodeStatus` MCP tool — Phase 4 carry-forward; behaviour and shape unchanged but now also emits a drawer audit event with `meta.tool: 'updateNodeStatus'` (D-09)
- [ ] **PLUG-AGENT-DELETE-01**: `deleteNode` MCP tool deletes a leaf node directly; non-leaf without `cascade: true` returns `cascade_required` with `data.childCount`; deleting the last remaining root returns `cannot_delete_last_root` (D-11); emits drawer event
- [ ] **PLUG-AGENT-FILE-01**: `saveFile` MCP tool synchronously flushes the autosave debouncer (Phase 3 D-13); emits drawer event with `nodeId: "__lifecycle__"`
- [ ] **PLUG-AGENT-FILE-02**: `saveFileAs` MCP tool writes the schema to an absolute path; the path must satisfy the existing Phase 3 03-04a allowlist (`isPathWithinMainDir` OR `dialogAllowlist`) — out-of-allowlist paths return `path_not_permitted` (D-13)
- [ ] **PLUG-AGENT-FILE-03**: `openFile` MCP tool auto-flushes pending autosave (D-12) then loads a roadmap from a path; allowlist-checked (D-13); does NOT trigger the Phase 3 external-edit-toast (in-process load)
- [ ] **PLUG-AGENT-TRANSPORT-01**: WebSocket request/response framing on the Bun side — inbound `{type:'request', id, method, params}` routed via `agentRequestHandler` to the renderer-store dispatcher; debounce-bypass (Phase 4 D-25 EventCoalescer is NOT engaged for agent requests)
- [ ] **PLUG-AGENT-TRANSPORT-02**: `wsClient.request(method, params): Promise<result>` — correlation-id pending map, 30 s default timeout, rejects all in-flight requests on socket close before scheduling reconnect
- [ ] **PLUG-AGENT-SAFETY-01**: Structured error taxonomy — every gate failure returns `{code, message, hint?, data?}` from the catalog: `app_not_running`, `no_file_loaded`, `node_not_found`, `cascade_required`, `cannot_delete_last_root`, `path_not_permitted`, `cross_ref_boundary`, `move_would_create_cycle`, `file_read_error`, `save_error`, `agent_api_disabled`, `unknown_tool`, `internal_error`
- [ ] **PLUG-AGENT-SAFETY-02**: Drawer audit log — every mutating tool call (create/update/delete/file-lifecycle) writes a synthetic `IntegrationEvent` with `source: 'claude-code'`, `meta.tool`, `meta.args`, `meta.label` into `eventLogStore` (D-09); read tools do NOT write
- [ ] **PLUG-AGENT-SAFETY-03**: Kill-switch setting `agentApi.enabled` in `.roadmap-settings.json` — when `false`, the Bun request handler returns `agent_api_disabled` for every tool BEFORE dispatching; event-push API remains active independently
```

ALSO update the Traceability table at the bottom of REQUIREMENTS.md: add a row `| PLUG-AGENT-READ-01 to PLUG-AGENT-SAFETY-03 | Phase 6 | Pending |` immediately after the `PACK-01 to PACK-06` row.

ALSO update the **Coverage** block: increase `v1 requirements: 61 total` → `v1 requirements: 84 total` and `Mapped to phases: 61` → `Mapped to phases: 84`.

DO NOT add any v2 entries. DO NOT touch the Out of Scope table. DO NOT activate PLUG-09's plugin/subscribe fields (Phase 4 D-26 still applies; Phase 6 stays parsed-but-inert per CONTEXT.md domain section).

Reference: each ID's source of authority is the matching D-NN in 06-CONTEXT.md `<decisions>`. The PLUG-AGENT-UPDATE-06 entry exists because Phase 4's `updateNodeStatus` tool gets a drawer-event side effect added in this phase (per RESEARCH §7), so the requirement needs a Phase-6 ID for traceability without renaming PLUG-08.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const ids=['PLUG-AGENT-READ-01','PLUG-AGENT-READ-02','PLUG-AGENT-READ-03','PLUG-AGENT-READ-04','PLUG-AGENT-READ-05','PLUG-AGENT-READ-06','PLUG-AGENT-CREATE-01','PLUG-AGENT-CREATE-02','PLUG-AGENT-UPDATE-01','PLUG-AGENT-UPDATE-02','PLUG-AGENT-UPDATE-03','PLUG-AGENT-UPDATE-04','PLUG-AGENT-UPDATE-05','PLUG-AGENT-UPDATE-06','PLUG-AGENT-DELETE-01','PLUG-AGENT-FILE-01','PLUG-AGENT-FILE-02','PLUG-AGENT-FILE-03','PLUG-AGENT-TRANSPORT-01','PLUG-AGENT-TRANSPORT-02','PLUG-AGENT-SAFETY-01','PLUG-AGENT-SAFETY-02','PLUG-AGENT-SAFETY-03'];for(const id of ids){if(!r.includes(id)){console.error('missing',id);process.exit(1);}}if(!r.includes('### Agentic Roadmap Authoring (PLUG-AGENT)')){console.error('missing heading');process.exit(1);}if(!r.includes('PLUG-AGENT-READ-01 to PLUG-AGENT-SAFETY-03'))console.warn('traceability row optional pattern not found');console.log('ok')"</automated>
  </verify>
  <done>REQUIREMENTS.md contains the new section, all 23 PLUG-AGENT-* IDs, the Traceability row, and updated coverage counts. `grep -c "^- \\[ \\] \\*\\*PLUG-AGENT-" .planning/REQUIREMENTS.md` returns exactly 23.</done>
</task>

<task type="auto">
  <name>Task 2: Extend shared/types.ts — agentRequest RPC entry + AppSettings.agentApi kill-switch</name>
  <files>shared/types.ts</files>
  <read_first>
    - shared/types.ts (lines 1-162 — read once; do NOT re-read)
    - .planning/phases/06-agentic-roadmap-authoring/06-PATTERNS.md §"shared/types.ts — add agentRequest RPC entry + agentApi settings field"
    - .planning/phases/06-agentic-roadmap-authoring/06-RESEARCH.md §3 (RPC Bridge Design) and §13 (Kill-Switch)
  </read_first>
  <action>
    Apply two edits to `shared/types.ts`:

    EDIT 1 — Inside the `AppSettings` interface (currently lines 29-37), add a new optional field AFTER the existing `eventApi?` block, mirroring its optional-object shape exactly. Final block:

```typescript
eventApi?: {
  /** User-specified WebSocket port override. When set, no auto-fallback on EADDRINUSE. */
  port?: number;
};
agentApi?: {
  /** When false, the Bun-side agent request handler short-circuits with `agent_api_disabled` BEFORE dispatching to the renderer store. The Phase 4 event-push API remains active. Default: undefined / true (enabled). Per Phase 6 RESEARCH §13 / D-15 discretion. */
  enabled?: boolean;
};
```

    EDIT 2 — Inside `RoadmapRPCType.bun.requests` (currently `loadFile`, `saveFile`, ..., `getEventApiState`), add a new entry AFTER `getEventApiState` and BEFORE the closing brace of `requests`. Use this exact shape (copy from RESEARCH §3 — single generic dispatcher per Option B):

```typescript
// Phase 6 — single dispatcher entry. The Bun-side agentRequestHandler forwards
// MCP tool calls into this RPC; the renderer dispatcher (mainview/rpc/agentRpcHandler.ts)
// routes by `tool` string into the correct roadmapStore action. One RPC entry
// keeps the shared contract surface lean; per-tool typing happens at the Zod
// validation boundaries (plugin tool-input schemas + renderer dispatcher).
agentRequest: {
  params: {
    tool: string;
    args: Record<string, unknown>;
  };
  response:
    | { ok: true; data: unknown }
    | { ok: false; error: string; code: string; hint?: string; data?: unknown };
};
```

    DO NOT change any existing entry. DO NOT add new `messages` entries (drawer events flow through the existing `pushEventLog` channel — see Plan 03). DO NOT add an `agentMutate` alias (per RESEARCH §3 Recommendation: single `agentRequest`, not 17 typed entries). DO NOT change the existing `IntegrationEvent` re-export pattern at lines 1-3.

    The new RPC entry follows existing structural conventions: snake-cased params object with `params` and `response` keys, response is a discriminated union on `ok`, comments explain phase-6 rationale.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('shared/types.ts','utf8');if(!t.includes('agentRequest:'))throw new Error('missing agentRequest');if(!t.includes('agentApi?:'))throw new Error('missing agentApi');if(!t.match(/response:\s*\|\s*\{\s*ok:\s*true;\s*data:\s*unknown\s*\}/))throw new Error('discriminated union shape mismatch');console.log('ok')" && bunx tsc --noEmit -p packages/desktop/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>`shared/types.ts` exports both additions. `bunx tsc --noEmit -p packages/desktop/tsconfig.json` is clean (no type errors introduced).</done>
</task>

<task type="auto">
  <name>Task 3: Extend packages/desktop/src/bun/eventSchema.ts — add AgentRequestSchema to the inbound parse union</name>
  <files>packages/desktop/src/bun/eventSchema.ts</files>
  <read_first>
    - packages/desktop/src/bun/eventSchema.ts (lines 1-67 — current 2-way union)
    - packages/desktop/tests/unit/bun/eventSchema.test.ts (existing test patterns to mirror)
    - .planning/phases/06-agentic-roadmap-authoring/06-PATTERNS.md §"packages/desktop/src/bun/eventSchema.ts — add AgentRequest to parse union"
    - .planning/phases/06-agentic-roadmap-authoring/06-RESEARCH.md §2 (WS framing) and §"Transport Design — Envelope Shapes"
  </read_first>
  <action>
    Add the `AgentRequestSchema` definition AFTER `EventFrameSchema` (currently lines 11-27) and BEFORE the existing `IncomingFrameSchema` line. Use this exact shape:

```typescript
// Phase 6 / D-15 — agent request envelope on the same WS channel. Three-way
// discriminator keeps a single transport while distinguishing producer events
// from agent RPC. The body uses an open `Record<string, unknown>` because each
// tool's `params` shape is validated downstream by the plugin's Zod tool-input
// schemas (plugins/claude-code/src/tools/schemas.ts) and re-checked at the
// renderer dispatcher boundary.
export const AgentRequestSchema = z.object({
  type: z.literal("request"),
  id: z.string().min(1),
  method: z.string().min(1),
  params: z.record(z.string(), z.unknown()),
});

export type AgentRequest = z.infer<typeof AgentRequestSchema>;
```

    Then update the existing `IncomingFrameSchema` line (currently line 29) — replace the 2-way union with a 3-way union:

```typescript
const IncomingFrameSchema = z.union([
  HelloFrameSchema,
  AgentRequestSchema,
  EventFrameSchema,
]);
```

    Then update the `parseIncoming` return-type signature (currently lines 53-56) so the success branch carries the wider union:

```typescript
export function parseIncoming(
  raw: string,
):
  | { ok: true; frame: HelloFrame | AgentRequest | EventFrame }
  | { ok: false; error: "malformed" } {
```

    The function body is UNCHANGED — `IncomingFrameSchema.safeParse` already handles the wider union; only the type annotation changes.

    DO NOT modify `EventFrameSchema`, `HelloFrameSchema`, `META_MAX_BYTES`, `classifyEventFrame`, or `Allowlist`. DO NOT add new exports beyond `AgentRequestSchema` and `AgentRequest`. DO NOT add `_error` classifications for agent requests (per RESEARCH §7 — agent ops succeed or return structured errors to the agent, never to the drawer error-classification surface).

    Match the existing file's style: 2-space indent (the file uses tabs — match it; sample lines 14-25 show tab indentation), Zod v4 `z.record(z.string(), z.unknown())` form (NOT `z.record(z.unknown())` — Phase 02 decision logged in STATE.md).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('packages/desktop/src/bun/eventSchema.ts','utf8');if(!t.includes('AgentRequestSchema'))throw new Error('AgentRequestSchema missing');if(!t.match(/z\.union\(\[\s*HelloFrameSchema,\s*AgentRequestSchema,\s*EventFrameSchema/))throw new Error('IncomingFrameSchema not 3-way');if(!t.includes('HelloFrame | AgentRequest | EventFrame'))throw new Error('parseIncoming return type not updated');if(!t.includes('export type AgentRequest'))throw new Error('AgentRequest type missing');console.log('ok')" &amp;&amp; bunx tsc --noEmit -p packages/desktop/tsconfig.json 2>&amp;1 | head -20</automated>
  </verify>
  <done>eventSchema.ts exports AgentRequestSchema + type, IncomingFrameSchema is 3-way, parseIncoming return signature updated. tsc clean. Existing eventSchema.test.ts still passes (run `bun run test:file packages/desktop/tests/unit/bun/eventSchema.test.ts` — wider union must still accept hello+event frames).</done>
</task>

<task type="auto">
  <name>Task 4: Create plugins/claude-code/src/tools/schemas.ts — Zod input schemas for all non-trivial agent tools</name>
  <files>plugins/claude-code/src/tools/schemas.ts</files>
  <read_first>
    - packages/core/src/schema.ts (verify exports of RoadmapNodeSchema, RoadmapSchemaSchema, StatusConfigSchema, TypeConfigSchema)
    - plugins/claude-code/src/server.ts (lines 26-40 — existing inputSchema pattern with `.describe()` on every field)
    - .planning/phases/06-agentic-roadmap-authoring/06-PATTERNS.md §"plugins/claude-code/src/tools/schemas.ts — new Zod schema file"
    - .planning/phases/06-agentic-roadmap-authoring/06-RESEARCH.md §"Tool Catalog" (the canonical Zod shape for every tool)
    - plugins/claude-code/package.json (verify zod ^4.3.6 + @roadraven/core import path)
  </read_first>
  <action>
    Create new file `plugins/claude-code/src/tools/schemas.ts`. Imports + every input schema below — use this exact content (tab-indented to match the package style):

```typescript
import { z } from "zod";
import {
  RoadmapNodeSchema,
  RoadmapSchemaSchema,
  StatusConfigSchema,
  TypeConfigSchema,
} from "@roadraven/core";

// Phase 6 — Zod input schemas for every MCP tool whose input shape is
// non-trivial. Trivial tools (no args: getRoadmap, getStatusConfig, getTypeConfig,
// getOpenFile, saveFile; single-id args: renameNode, updateNodeType,
// updateNodeNotes, updateNodeStatus) declare their inputSchema inline at the
// `server.registerTool` call site in `../server.ts` to keep this file scoped to
// the schemas that actually need composition / pickers / nullable PATCH
// semantics. All schemas re-use `RoadmapNodeSchema` / config schemas from
// @roadraven/core where possible (RESEARCH §11) so a single source of truth
// drives validation across producer and agent paths.

// -- Read tools --

export const GetNodeInputSchema = z.object({
  nodeId: z.string().uuid().describe("UUID of the node to fetch"),
});

export const FindNodesInputSchema = z.object({
  titleContains: z.string().optional().describe("Case-insensitive substring against node.title"),
  status: z.string().optional().describe("Filter by exact status id"),
  type: z.string().optional().describe("Filter by exact type string"),
  metaKey: z.string().optional().describe("Filter to nodes whose metadata has this key"),
  metaValue: z.unknown().optional().describe("When set, metaKey must equal this value (deep-equal)"),
  parentId: z.string().uuid().optional().describe("Filter to direct children of this parent UUID"),
});

// -- Create tools --

export const CreateNodeInputSchema = z.object({
  parentId: z.string().uuid().describe("UUID of the parent node to attach this child to"),
  title: z.string().min(1).max(200).describe("Node title (1–200 chars)"),
  type: z.string().optional().describe("Type string — usually one of typeConfig[].id"),
  status: z.string().optional().describe("Status id — defaults to 'not-started' (matches makeNewNode in roadmapStore)"),
  notes: z.string().optional().describe("Markdown notes attached to the node"),
  metadata: z.record(z.string(), z.unknown()).optional().describe("Free-form key-value metadata"),
});

export const CreateRoadmapInputSchema = z.object({
  title: z.string().min(1).optional().describe("Roadmap title — defaults to 'Untitled Roadmap'"),
  statusConfig: z.array(StatusConfigSchema).optional().describe("Custom status palette; defaults to the not-started/in-progress/completed/blocked set"),
  typeConfig: z.array(TypeConfigSchema).optional().describe("Custom node-type palette"),
});

// -- Update tools --

// updateNodeMetadata uses PATCH semantics (D-04): null deletes the key, unlisted keys preserved.
// Note: z.unknown().nullable() means the *value* can be null OR any other type — combined with
// z.record(z.string(), …) the patch object is `Record<string, unknown | null>`.
export const UpdateNodeMetadataInputSchema = z.object({
  nodeId: z.string().uuid(),
  patch: z.record(z.string(), z.unknown().nullable()).describe(
    "Shallow PATCH — null value deletes that key from node.metadata; unlisted keys are preserved",
  ),
});

export const MoveNodeInputSchema = z.object({
  nodeId: z.string().uuid(),
  newParentId: z.string().uuid().describe("UUID of the new parent"),
  position: z
    .number()
    .int()
    .min(0)
    .optional()
    .describe("Insertion index among the new parent's children; omit to append at end"),
});

// -- Delete tools --

export const DeleteNodeInputSchema = z.object({
  nodeId: z.string().uuid(),
  cascade: z
    .boolean()
    .optional()
    .describe("Required (true) for non-leaf nodes — see D-11. Leaf delete needs no cascade flag."),
});

// -- File-lifecycle tools --

export const SaveFileAsInputSchema = z.object({
  path: z
    .string()
    .min(1)
    .describe(
      "Absolute path. Must be inside the directory of the currently loaded file, or a previously user-picked path in this session, or a recent-files entry — see Phase 3 03-04a allowlist.",
    ),
});

export const OpenFileInputSchema = z.object({
  path: z
    .string()
    .min(1)
    .describe("Absolute path. Same allowlist as saveFileAs (D-13). Auto-flushes pending autosave before loading."),
});

// Re-export the core Zod schemas the renderer dispatcher needs to verify against
// when constructing payloads (read tools return RoadmapNode / RoadmapSchema shapes).
export { RoadmapNodeSchema, RoadmapSchemaSchema, StatusConfigSchema, TypeConfigSchema };
```

    Create the directory `plugins/claude-code/src/tools/` (does not yet exist). Do NOT add output Zod schemas (MCP tool responses are text — the renderer side wraps results in `{ content: [{ type: "text", text: JSON.stringify(...) }] }`; output validation happens implicitly via the renderer dispatcher's typed return). Do NOT include schemas for trivial tools (`getRoadmap`, `getStatusConfig`, `getTypeConfig`, `getOpenFile`, `saveFile`, `renameNode`, `updateNodeType`, `updateNodeNotes`, `updateNodeStatus`) — those are inline at the registration site to keep server.ts self-documenting. Do NOT use `z.string().enum([...])` for status/type fields — user-defined statusConfig means open strings (Phase 4 D-26 / RESEARCH §11 final paragraph).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='plugins/claude-code/src/tools/schemas.ts';const t=fs.readFileSync(p,'utf8');const must=['CreateNodeInputSchema','CreateRoadmapInputSchema','GetNodeInputSchema','FindNodesInputSchema','UpdateNodeMetadataInputSchema','MoveNodeInputSchema','DeleteNodeInputSchema','SaveFileAsInputSchema','OpenFileInputSchema'];for(const x of must){if(!t.includes('export const '+x))throw new Error('missing '+x);}if(!t.includes('z.unknown().nullable()'))throw new Error('PATCH null-shape missing');if(!t.includes('@roadraven/core'))throw new Error('core import missing');console.log('ok')" &amp;&amp; bunx tsc --noEmit -p plugins/claude-code/tsconfig.json 2>&amp;1 | head -20</automated>
  </verify>
  <done>`plugins/claude-code/src/tools/schemas.ts` exports the 9 listed schemas + 4 re-exports, imports from `@roadraven/core`, no type errors. The file is the single import target for Plan 04 server.ts edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Scaffold all 6 Wave 0 test files (failing/pending RED state)</name>
  <files>
    plugins/claude-code/tests/wsClient.request.test.ts,
    plugins/claude-code/tests/agent-tools.contract.test.ts,
    packages/desktop/tests/unit/bun/agentRequestHandler.test.ts,
    packages/desktop/tests/unit/store/roadmapStore.moveNode.test.ts,
    packages/desktop/tests/unit/ui/agentRpcHandler.test.ts,
    plugins/claude-code/tests/scaffold.e2e.test.ts
  </files>
  <behavior>
    Each scaffold file MUST:
    - Be a valid vitest file that vitest discovers (filename matches glob).
    - Use `describe`/`it.todo` for behaviours not yet implementable (RED-as-pending), and `describe`/`it` with intentionally-failing assertions for behaviours that can be expressed against the contracts already added in Tasks 1-4.
    - Include the imports the future implementation will need (commented out where the implementation file does not yet exist) so the executor in later plans only needs to uncomment + flesh out.
    - Be discoverable by `bun run test:file <filename>` (i.e. live under the expected vitest glob in the package's vitest.config.ts).
  </behavior>
  <read_first>
    - plugins/claude-code/tests/wsClient.test.ts (existing MockWebSocket pattern — copy `MockWebSocket.instances`, `addEventListener`, `emit`, `send.mock.calls.at(-1)` shape)
    - plugins/claude-code/tests/sentinel.test.ts (existing describe/it style)
    - packages/desktop/tests/unit/bun/eventSchema.test.ts (existing Bun-side describe/it style + Zod parser tests)
    - packages/desktop/tests/unit/store/roadmapStore.mutations.test.ts (existing makeTestSchema fixture pattern)
    - .planning/phases/06-agentic-roadmap-authoring/06-PATTERNS.md §"Test Patterns" (template for each scaffold)
    - .planning/phases/06-agentic-roadmap-authoring/06-VALIDATION.md §"Wave 0 Requirements" + §"Phase Requirements → Test Map"
  </read_first>
  <action>
    Create 6 vitest scaffold files. Each starts with a header comment naming the requirement IDs it covers and links to the implementing plan number. Use `it.todo("...")` for cases that need future modules (avoids RED noise that would block the suite); use real failing `expect(...).toBe(...)` for any assertion expressible against contracts already on disk.

    FILE 1 — `plugins/claude-code/tests/wsClient.request.test.ts`:
```typescript
// Phase 6 / PLUG-AGENT-TRANSPORT-02 — wsClient.request() correlation, timeout,
// reconnect-resilience. Implementation lands in 06-02-PLAN.md (wsClient.ts edits).
// MockWebSocket pattern: copy from plugins/claude-code/tests/wsClient.test.ts.

import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
// import { createWsClient } from "../src/wsClient";
// import { readSentinel } from "../src/sentinel";

describe("WsClient request/response (Phase 6)", () => {
  beforeEach(() => {
    // TODO 06-02: copy MockWebSocket setup from wsClient.test.ts
  });
  afterEach(() => {
    // TODO 06-02: copy MockWebSocket teardown
  });

  it.todo("request() sends {type:'request', id, method, params} envelope on the open socket");
  it.todo("request() resolves when a matching {type:'response', id, result} arrives");
  it.todo("request() rejects with code/hint/data when {type:'response', id, error:{...}} arrives");
  it.todo("request() rejects with 'timed out' after 30s with no response");
  it.todo("request() rejects all in-flight requests on socket close before scheduling reconnect (L-01 mitigation)");
  it.todo("ignores non-JSON messages and messages without a matching pending id");

  it("scaffold compiles — placeholder assertion", () => {
    expect(true).toBe(true);
  });
});
```

    FILE 2 — `plugins/claude-code/tests/agent-tools.contract.test.ts`:
```typescript
// Phase 6 / PLUG-AGENT-READ-* / CREATE-* / UPDATE-* / DELETE-* / FILE-* — Zod
// schema contract tests for the ~17 net-new MCP tool inputs. The schemas live in
// plugins/claude-code/src/tools/schemas.ts (Plan 06-01 Task 4).

import { describe, expect, it } from "vitest";
import {
  CreateNodeInputSchema,
  CreateRoadmapInputSchema,
  DeleteNodeInputSchema,
  FindNodesInputSchema,
  GetNodeInputSchema,
  MoveNodeInputSchema,
  OpenFileInputSchema,
  SaveFileAsInputSchema,
  UpdateNodeMetadataInputSchema,
} from "../src/tools/schemas";

describe("createNode input schema", () => {
  it("accepts a minimal valid payload", () => {
    const r = CreateNodeInputSchema.safeParse({
      parentId: "00000000-0000-4000-8000-000000000000",
      title: "x",
    });
    expect(r.success).toBe(true);
  });
  it("rejects empty title", () => {
    const r = CreateNodeInputSchema.safeParse({
      parentId: "00000000-0000-4000-8000-000000000000",
      title: "",
    });
    expect(r.success).toBe(false);
  });
  it("rejects non-uuid parentId", () => {
    const r = CreateNodeInputSchema.safeParse({ parentId: "not-a-uuid", title: "x" });
    expect(r.success).toBe(false);
  });
});

describe("findNodes input schema (D-03 AND-combined filter)", () => {
  it("accepts an empty object (returns all)", () => {
    expect(FindNodesInputSchema.safeParse({}).success).toBe(true);
  });
  it("accepts each filter field individually", () => {
    expect(FindNodesInputSchema.safeParse({ titleContains: "x" }).success).toBe(true);
    expect(FindNodesInputSchema.safeParse({ status: "in-progress" }).success).toBe(true);
    expect(FindNodesInputSchema.safeParse({ type: "task" }).success).toBe(true);
    expect(FindNodesInputSchema.safeParse({ metaKey: "priority" }).success).toBe(true);
    expect(FindNodesInputSchema.safeParse({ metaValue: 1 }).success).toBe(true);
    expect(FindNodesInputSchema.safeParse({ parentId: "00000000-0000-4000-8000-000000000000" }).success).toBe(true);
  });
});

describe("updateNodeMetadata input schema (D-04 PATCH semantics)", () => {
  it("accepts string keys with non-null values", () => {
    expect(UpdateNodeMetadataInputSchema.safeParse({
      nodeId: "00000000-0000-4000-8000-000000000000",
      patch: { priority: "P0" },
    }).success).toBe(true);
  });
  it("accepts null values for key deletion", () => {
    expect(UpdateNodeMetadataInputSchema.safeParse({
      nodeId: "00000000-0000-4000-8000-000000000000",
      patch: { priority: null },
    }).success).toBe(true);
  });
});

describe("deleteNode input schema (D-11 cascade gate)", () => {
  it("accepts payload without cascade (leaf delete)", () => {
    expect(DeleteNodeInputSchema.safeParse({ nodeId: "00000000-0000-4000-8000-000000000000" }).success).toBe(true);
  });
  it("accepts cascade:true (non-leaf delete)", () => {
    expect(DeleteNodeInputSchema.safeParse({ nodeId: "00000000-0000-4000-8000-000000000000", cascade: true }).success).toBe(true);
  });
});

describe("moveNode input schema", () => {
  it("accepts mandatory ids without position", () => {
    expect(MoveNodeInputSchema.safeParse({
      nodeId: "00000000-0000-4000-8000-000000000000",
      newParentId: "00000000-0000-4000-8000-000000000001",
    }).success).toBe(true);
  });
  it("rejects negative position", () => {
    expect(MoveNodeInputSchema.safeParse({
      nodeId: "00000000-0000-4000-8000-000000000000",
      newParentId: "00000000-0000-4000-8000-000000000001",
      position: -1,
    }).success).toBe(false);
  });
});

describe("file-lifecycle input schemas", () => {
  it("openFile requires a non-empty path", () => {
    expect(OpenFileInputSchema.safeParse({ path: "" }).success).toBe(false);
    expect(OpenFileInputSchema.safeParse({ path: "/tmp/r.json" }).success).toBe(true);
  });
  it("saveFileAs requires a non-empty path", () => {
    expect(SaveFileAsInputSchema.safeParse({ path: "" }).success).toBe(false);
    expect(SaveFileAsInputSchema.safeParse({ path: "/tmp/r.json" }).success).toBe(true);
  });
});

describe("getNode + createRoadmap input schemas", () => {
  it("getNode requires a uuid", () => {
    expect(GetNodeInputSchema.safeParse({ nodeId: "00000000-0000-4000-8000-000000000000" }).success).toBe(true);
    expect(GetNodeInputSchema.safeParse({ nodeId: "x" }).success).toBe(false);
  });
  it("createRoadmap accepts an empty payload", () => {
    expect(CreateRoadmapInputSchema.safeParse({}).success).toBe(true);
  });
});
```

    FILE 3 — `packages/desktop/tests/unit/bun/agentRequestHandler.test.ts`:
```typescript
// Phase 6 / PLUG-AGENT-TRANSPORT-01, SAFETY-01, SAFETY-03, DELETE-01, UPDATE-04,
// UPDATE-05, FILE-03 — Bun request router. Implementation: Plan 06-02.

import { describe, expect, it } from "vitest";
// import { agentRequestHandler } from "../../../src/bun/agentRequestHandler";

describe("agentRequestHandler — kill-switch (D-15 / SAFETY-03)", () => {
  it.todo("returns agent_api_disabled when settings.agentApi.enabled === false");
  it.todo("dispatches normally when agentApi.enabled is undefined or true");
});

describe("agentRequestHandler — error taxonomy (SAFETY-01)", () => {
  it.todo("returns no_file_loaded when schema is null");
  it.todo("returns node_not_found for unknown nodeId");
  it.todo("returns unknown_tool when method is not in the dispatch switch");
  it.todo("returns internal_error wrapping unhandled exceptions");
});

describe("agentRequestHandler — deleteNode gates (D-11 / DELETE-01)", () => {
  it.todo("returns cascade_required with data.childCount for non-leaf without cascade:true");
  it.todo("allows cascade:true on non-leaf");
  it.todo("returns cannot_delete_last_root when nodes.length === 1 and target is root");
});

describe("agentRequestHandler — updateNodeMetadata PATCH (D-04 / UPDATE-04)", () => {
  it.todo("null value deletes the key");
  it.todo("unlisted keys are preserved across the patch");
  it.todo("returns the final metadata object in result.data");
});

describe("agentRequestHandler — moveNode (UPDATE-05)", () => {
  it.todo("returns move_would_create_cycle when newParentId is a descendant of nodeId");
  it.todo("returns cross_ref_boundary when newParentId is owned by a different ref file (Bun-side ownership check, RESEARCH L-08 Option B)");
});

describe("agentRequestHandler — file-lifecycle gates (FILE-03 / D-12)", () => {
  it.todo("openFile flushes pending autosave before loading");
  it.todo("openFile returns path_not_permitted when path is outside allowlist");
  it.todo("saveFileAs returns path_not_permitted when path is outside allowlist");
});

it("scaffold compiles — placeholder", () => {
  expect(true).toBe(true);
});
```

    FILE 4 — `packages/desktop/tests/unit/store/roadmapStore.moveNode.test.ts`:
```typescript
// Phase 6 / PLUG-AGENT-UPDATE-05 — new roadmapStore.moveNode action.
// Implementation: Plan 06-02 Task adding moveNode to roadmapStore.ts.
// Cycle detection lives in Bun-side handler (per RESEARCH L-08 Option B), so
// this file tests the structural mutation only — caller is expected to have
// validated cycle freedom before invoking.

import { describe, expect, it } from "vitest";
// import { useRoadmapStore } from "../../../src/mainview/store/roadmapStore";

describe("roadmapStore.moveNode (Phase 6)", () => {
  it.todo("re-parents a leaf node to a different parent at end position by default");
  it.todo("inserts at explicit position (0 = first child) when position is provided");
  it.todo("clamps position to children.length when position > children.length");
  it.todo("bumps dataKey (structural change, unlike moveNodeUp/Down which preserve nodeIndex)");
  it.todo("does nothing when nodeId is not found");
  it.todo("does nothing when newParentId is not found");
});

it("scaffold compiles — placeholder", () => {
  // The actual import lands when moveNode is exported from roadmapStore.ts in 06-02.
  expect(true).toBe(true);
});
```

    FILE 5 — `packages/desktop/tests/unit/ui/agentRpcHandler.test.ts`:
```typescript
// Phase 6 / PLUG-AGENT-SAFETY-02 — renderer dispatcher writes drawer events for
// mutating tools, never for read tools. Implementation: Plan 06-03.

import { afterEach, describe, expect, it } from "vitest";
// import { handleAgentRequest } from "../../../src/mainview/rpc/agentRpcHandler";
// import { useEventLogStore } from "../../../src/mainview/store/eventLogStore";
// import { useRoadmapStore } from "../../../src/mainview/store/roadmapStore";

describe("agentRpcHandler — drawer audit events (D-09)", () => {
  afterEach(() => {
    // TODO 06-03: reset useRoadmapStore + useEventLogStore between tests
  });

  it.todo("createNode appends an IntegrationEvent with source='claude-code' and meta.tool='createNode'");
  it.todo("renameNode / updateNodeType / updateNodeNotes / updateNodeMetadata each append a drawer event");
  it.todo("deleteNode appends a drawer event with the deleted node's id");
  it.todo("file-lifecycle tools (saveFile, saveFileAs, openFile) append with nodeId='__lifecycle__'");
  it.todo("read tools (getRoadmap, getNode, findNodes, getStatusConfig, getTypeConfig, getOpenFile) do NOT append drawer events");
  it.todo("updateNodeStatus (Phase 4 carry-forward) NOW appends a drawer event with meta.tool='updateNodeStatus' (UPDATE-06)");
});

describe("agentRpcHandler — PATCH semantics (D-04 / UPDATE-04)", () => {
  it.todo("updateNodeMetadata with patch={priority: null} deletes the priority key");
  it.todo("updateNodeMetadata with patch={a:1} preserves an existing {b:2} key");
});

it("scaffold compiles — placeholder", () => {
  expect(true).toBe(true);
});
```

    FILE 6 — `plugins/claude-code/tests/scaffold.e2e.test.ts`:
```typescript
// Phase 6 — full scaffold story end-to-end. The named ROADMAP user story:
// "scaffold a roadmap for migrating service X". Implementation: Plan 06-05.
//
// This test exercises the full chain: MCP tool call → wsClient.request →
// Bun agentRequestHandler → renderer agentRequest RPC → store mutation →
// autosave → file on disk. Requires either a real running app (manual UAT)
// or a mock harness that stitches the layers; pick the cheapest path in 06-05.

import { describe, expect, it } from "vitest";

describe("Phase 6 E2E — scaffold a roadmap (named user story)", () => {
  it.todo("createRoadmap → 30× createNode for service X migration steps → tree assembled in store");
  it.todo("autosave fires within ~2s of last createNode (Phase 3 D-13 debounce)");
  it.todo("after stop() and reload, the assembled tree persists on disk");
  it.todo("every mutation produced a corresponding drawer audit event with source='claude-code'");
});

it("scaffold compiles — placeholder", () => {
  expect(true).toBe(true);
});
```

    Match each package's tab/space + import-path conventions. Verify that vitest discovers each file with `bun run test:file <path>`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['plugins/claude-code/tests/wsClient.request.test.ts','plugins/claude-code/tests/agent-tools.contract.test.ts','packages/desktop/tests/unit/bun/agentRequestHandler.test.ts','packages/desktop/tests/unit/store/roadmapStore.moveNode.test.ts','packages/desktop/tests/unit/ui/agentRpcHandler.test.ts','plugins/claude-code/tests/scaffold.e2e.test.ts'];for(const f of files){if(!fs.existsSync(f))throw new Error('missing '+f);const t=fs.readFileSync(f,'utf8');if(!t.includes('describe('))throw new Error('no describe in '+f);if(!/(it\.todo|it\()/.test(t))throw new Error('no it() in '+f);}console.log('ok')" &amp;&amp; bun run test:file plugins/claude-code/tests/agent-tools.contract.test.ts</automated>
  </verify>
  <done>All 6 files exist, vitest can discover them, the agent-tools.contract test passes (uses already-created Zod schemas from Task 4); the other 5 either pass placeholder assertions or have `it.todo` for future implementation. No test fails — RED state is encoded as `.todo`, not failing assertions, so CI stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP host (Claude Code) → MCP wrapper (`server.ts`) | Untrusted tool args from agent reasoning; Zod schemas in this plan are the first validation layer |
| MCP wrapper (Bun child) → desktop Bun WS server | Localhost-only, sentinel-gated boundary; no auth (Phase 4 D-03) |
| `shared/types.ts` cross-process contract | Type-only — no runtime mutation, but shape mismatch between Bun and renderer is a footgun (TS catches at build time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Tool input args (e.g. crafted `parentId` to `createNode`) | mitigate | Zod schemas in `tools/schemas.ts` reject malformed payloads before they leave the MCP wrapper. UUID-shape on every nodeId field (`z.string().uuid()`); `min(1)` on every path / title field. |
| T-06-02 | Information Disclosure | `findNodes` filter object leaking via crafted `metaValue` (e.g. function reference) | mitigate | `metaValue: z.unknown()` is deliberately permissive at the schema layer; the renderer dispatcher does deep-equal in JS — no eval, no string templating, no SQL. Acceptable: agent already has read access to all metadata via `getNode`/`getRoadmap`. |
| T-06-03 | Denial of Service | Oversized `patch` object on `updateNodeMetadata` | accept | Zod has no upper bound on record size in this plan. Rationale: the schema is identical to existing `RoadmapNodeSchema.metadata` which is also unbounded; if a real attack surface emerges, add `.refine(size < 8KB)` (matches Phase 4 META_MAX_BYTES pattern). Low risk on a localhost single-user boundary. |
| T-06-04 | Spoofing | Type contract drift between Bun and renderer (`agentRequest.params` mismatch) | mitigate | Single `RoadmapRPCType` import on both sides; `bunx tsc --noEmit` in `bun run verify` blocks any drift before merge. |
| T-06-05 | Elevation of Privilege | Kill-switch bypass via WS protocol manipulation | mitigate | Kill-switch field defined in this plan; the actual gate check lands in Plan 02's `agentRequestHandler.ts` BEFORE dispatch, so a crafted `{type:'request'}` cannot escape the check. Settings file has the same on-disk write surface as the rest of `.roadmap-settings.json` (no new attack surface). |
</threat_model>

<verification>
- `node -e "..."` invariant scans pass for each file (REQUIREMENTS.md, types.ts, eventSchema.ts, schemas.ts).
- `bunx tsc --noEmit -p packages/desktop/tsconfig.json` exits 0.
- `bunx tsc --noEmit -p plugins/claude-code/tsconfig.json` exits 0.
- `bun run test:file plugins/claude-code/tests/agent-tools.contract.test.ts` passes (validates Zod schemas).
- `bun run test:file packages/desktop/tests/unit/bun/eventSchema.test.ts` passes (existing tests still green after the 3-way union extension).
- `grep -c "^- \\[ \\] \\*\\*PLUG-AGENT-" .planning/REQUIREMENTS.md` returns 23.
- `bun run test:lint` clean on all touched files.
</verification>

<success_criteria>
- 23 PLUG-AGENT-* requirement IDs in REQUIREMENTS.md mapped to Phase 6 in the Traceability table.
- `agentRequest` RPC entry in `RoadmapRPCType.bun.requests` with the discriminated-union response shape from RESEARCH §3.
- `agentApi.enabled?: boolean` in `AppSettings`.
- `AgentRequestSchema` exported from `eventSchema.ts`; `IncomingFrameSchema` is a 3-way union.
- `plugins/claude-code/src/tools/schemas.ts` exports the 9 listed schemas; agent-tools.contract.test.ts passes against them.
- 6 Wave 0 test scaffolds exist (5 with `it.todo`, 1 with passing assertions).
- `bun run verify` is still green (no regressions to existing 223+ tests).
</success_criteria>

<output>
After completion, create `.planning/phases/06-agentic-roadmap-authoring/06-01-SUMMARY.md` per the standard summary template, listing:
- Files modified (8)
- Files created (5)
- The exact PLUG-AGENT-* ID list (23 entries)
- The agentRequest RPC contract surface
- Wave 1 enablement notes (Plan 02 + 03 can now write code against the contracts)
</output>
</content>
</invoke>