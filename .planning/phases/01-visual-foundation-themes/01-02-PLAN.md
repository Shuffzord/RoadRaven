---
phase: 01-visual-foundation-themes
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/desktop/src/mainview/App.tsx
  - packages/desktop/src/mainview/components/TopBar.tsx
  - packages/desktop/src/mainview/components/Sidebar.tsx
  - packages/desktop/src/mainview/components/Canvas.tsx
  - packages/desktop/src/mainview/components/RoadmapNode.tsx
  - packages/desktop/src/mainview/components/SidePanel.tsx
  - packages/desktop/src/mainview/components/StatusBar.tsx
  - packages/desktop/src/mainview/components/ConfigPanel.tsx
  - packages/desktop/tests/unit/ui/components.test.tsx
autonomous: false
requirements:
  - THEME-05

must_haves:
  truths:
    - "App shell visually matches variant-c-merged.html layout structure"
    - "Every component uses only --rv-* token-based Tailwind classes — zero hardcoded color values"
    - "TopBar shows brand, action buttons, search, view controls, layout toggle, theme switcher with 4 options (dark, light, high-contrast, system per D-04)"
    - "Sidebar collapses from 220px to 48px with 200ms transition"
    - "Canvas shows dot-grid background pattern"
    - "Node card has 4px left status stripe, title, and status badge pill"
    - "SidePanel skeleton has header with close button, field labels, notes area"
    - "StatusBar shows three-section flex layout"
    - "ConfigPanel floats over canvas with toggle button"
    - "All interactive elements have correct ARIA attributes"
  artifacts:
    - path: "packages/desktop/src/mainview/components/TopBar.tsx"
      provides: "50px top bar with brand, actions, search, view controls, theme switcher (4 options)"
      exports: ["TopBar"]
    - path: "packages/desktop/src/mainview/components/Sidebar.tsx"
      provides: "220px/48px collapsible sidebar with sections"
      exports: ["Sidebar"]
    - path: "packages/desktop/src/mainview/components/Canvas.tsx"
      provides: "Dot-grid canvas area"
      exports: ["Canvas"]
    - path: "packages/desktop/src/mainview/components/RoadmapNode.tsx"
      provides: "Node card with status stripe + badge"
      exports: ["RoadmapNode"]
    - path: "packages/desktop/src/mainview/components/SidePanel.tsx"
      provides: "340px right panel skeleton"
      exports: ["SidePanel"]
    - path: "packages/desktop/src/mainview/components/StatusBar.tsx"
      provides: "32px status bar"
      exports: ["StatusBar"]
    - path: "packages/desktop/src/mainview/components/ConfigPanel.tsx"
      provides: "260px floating config panel"
      exports: ["ConfigPanel"]
  key_links:
    - from: "TopBar.tsx"
      to: "useTheme hook"
      via: "theme switcher buttons call setTheme"
      pattern: "setTheme"
    - from: "App.tsx"
      to: "all components"
      via: "grid layout composition"
      pattern: "gridArea"

user_setup: []
---

<objective>
Build all app shell components (TopBar, Sidebar, Canvas, RoadmapNode, SidePanel, StatusBar, ConfigPanel) as static skeletons matching the variant-c-merged.html design reference, using exclusively --rv-* Tailwind token classes.

Purpose: Delivers the complete visual shell that proves the token system works across all UI regions. Components are static skeletons in Phase 1 — real data wiring happens in Phase 2.

Output: All 7 components rendered in the app grid layout; theme switching changes all component colors; CI grep confirms zero hardcoded colors.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-themes/01-CONTEXT.md
@.planning/phases/01-visual-foundation-themes/01-UI-SPEC.md
@.planning/phases/01-visual-foundation-themes/01-RESEARCH.md
@.planning/phases/01-visual-foundation-themes/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor uses these directly -->
From store/themeStore.ts:
```typescript
export type ThemePreference = "dark" | "light" | "high-contrast" | "system";
export type ResolvedTheme = "dark" | "light" | "high-contrast";
export const useThemeStore = create<ThemeState>(...);
```

From hooks/useTheme.ts:
```typescript
export function useTheme(): {
  theme: ResolvedTheme;
  preference: ThemePreference;
  setTheme: (pref: ThemePreference) => void;
};
```

Token classes available (from index.css @theme):
- Backgrounds: bg-rv-bg-base, bg-rv-bg-surface, bg-rv-bg-elevated, bg-rv-bg-hover, bg-rv-bg-active, bg-rv-bg-input, bg-rv-bg-canvas, bg-rv-bg-toolbar, bg-rv-bg-panel, bg-rv-bg-statusbar, bg-rv-bg-node, bg-rv-bg-node-hover, bg-rv-bg-config
- Text: text-rv-text-primary, text-rv-text-secondary, text-rv-text-tertiary, text-rv-text-on-accent
- Borders: border-rv-border, border-rv-border-subtle, border-rv-border-focus
- Accent: bg-rv-accent, bg-rv-accent-hover, bg-rv-accent-muted, border-rv-accent-border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TopBar + Sidebar + Canvas + StatusBar + ConfigPanel components</name>
  <files>
    packages/desktop/src/mainview/components/TopBar.tsx,
    packages/desktop/src/mainview/components/Sidebar.tsx,
    packages/desktop/src/mainview/components/Canvas.tsx,
    packages/desktop/src/mainview/components/StatusBar.tsx,
    packages/desktop/src/mainview/components/ConfigPanel.tsx,
    packages/desktop/src/mainview/App.tsx
  </files>
  <read_first>
    packages/desktop/src/mainview/index.css,
    packages/desktop/src/mainview/App.tsx,
    packages/desktop/src/mainview/hooks/useTheme.ts,
    packages/desktop/src/mainview/store/themeStore.ts,
    .planning/phases/01-visual-foundation-themes/01-UI-SPEC.md
  </read_first>
  <action>
    Build all components per UI-SPEC specifications. Per D-03 and D-13, use ONLY Tailwind --rv-* token classes — zero hardcoded hex/rgb values in component files.

    **TopBar.tsx** (per UI-SPEC TopBar section):
    - Height 50px, bg-rv-bg-toolbar (aliased to bg-rv-bg-surface), bottom border with border-rv-border
    - Padding 0 12px, gap 4px, z-index 100, user-select none
    - role="toolbar", aria-label="Main toolbar"
    - Brand section: 20x20 inline SVG icon colored with text-rv-accent, "RoadRaven" text 14px font-semibold tracking-tight text-rv-text-primary
    - Vertical separator: 1px wide, 24px tall, bg-rv-border, margin 0 6px
    - Action buttons (New, Open): padding 5px 10px, rounded-[6px], 12px font-semibold, text-rv-text-secondary, hover:bg-rv-bg-hover hover:text-rv-text-primary transition-all duration-150. Icons 15x15 inline SVG (file-plus, folder)
    - Search bar: width 220px, height 30px, bg-rv-bg-input, border border-rv-border, rounded-lg, placeholder "Search nodes..." in text-rv-text-tertiary, Ctrl+F kbd hint (11px, bg-rv-bg-elevated, rounded-[3px], text-rv-text-tertiary). Container: role="search", input: aria-label="Search nodes"
    - Fit button: same style as action buttons, label "Fit"
    - Zoom buttons (-, +): 26x26px, rounded-[5px], text-rv-text-secondary, hover:bg-rv-bg-hover, 14x14 icons
    - Layout toggle (TB/LR): container bg-rv-bg-input border border-rv-border rounded-[6px] h-[28px]; options padding 0 10px, 11px font-semibold; inactive: text-rv-text-tertiary, hover:text-rv-text-secondary; active: bg-rv-accent-muted text-rv-accent transition duration-150. role="radiogroup" aria-label="Tree layout direction", each option role="radio" aria-checked
    - Theme switcher (per D-04 — FOUR options: dark, light, high-contrast, system): same container style as layout toggle; 4 buttons each 30x28px, 14x14 icons:
      - Dark (moon icon): calls setTheme("dark")
      - Light (sun icon): calls setTheme("light")
      - High Contrast (eye icon): calls setTheme("high-contrast")
      - System (computer/monitor icon): calls setTheme("system")
      - Inactive state: text-rv-text-tertiary, hover:text-rv-text-secondary
      - Active state: bg-rv-accent-muted text-rv-accent
      - Active detection: compare useTheme().preference (not resolvedTheme) against each button's value — e.g., "system" button is active when preference === "system", even if resolvedTheme is "dark"
      - role="radiogroup" aria-label="Theme switcher", each button role="radio" aria-checked={preference === buttonValue}
    - Settings gear button: 30x30px, rounded-[6px], 16x16 icon, text-rv-text-tertiary, hover:bg-rv-bg-hover hover:text-rv-text-primary, aria-label="Settings"

    **Sidebar.tsx** (per UI-SPEC Sidebar section):
    - Width 220px expanded, 48px collapsed (local useState for collapse state)
    - Transition: width 200ms ease (CSS transition-property: width; transition-duration: 200ms)
    - bg-rv-bg-surface, right border border-rv-border, z-index 50
    - role="navigation", aria-label="Sidebar navigation"
    - Header: h-[40px], padding 10px 12px, bottom border; title "Explorer" 11px font-semibold uppercase tracking-[0.05em] text-rv-text-tertiary (hidden when collapsed); collapse button 24x24 rounded-[4px] text-rv-text-tertiary hover:bg-rv-bg-hover hover:text-rv-text-primary, icon rotates 180deg when collapsed (transform rotate-180 duration-200), aria-label="Collapse sidebar" title="Ctrl+B"
    - Content: padding 8px 0, overflow-y auto
    - Section headers: "Recent Files", "Outline" — 11px font-semibold uppercase tracking-[0.06em] text-rv-text-tertiary padding 6px 14px
    - File items (static samples): padding 5px 14px gap-2, text-rv-text-secondary 12px, hover:bg-rv-bg-hover hover:text-rv-text-primary, 14x14 file icon text-rv-text-tertiary. When collapsed: padding 6px centered, text hidden
    - Bottom section: top border, padding 8px. Buttons: padding 6px 8px, rounded-[6px], 12px, text-rv-text-tertiary, hover:bg-rv-bg-hover hover:text-rv-text-primary, 14x14 icons. Collapsed: centered, label hidden

    **Canvas.tsx** (per UI-SPEC Canvas section):
    - bg-rv-bg-canvas, relative, overflow hidden
    - Dot grid via CSS: `background-image: radial-gradient(circle, var(--rv-dot-grid) 1px, transparent 1px); background-size: 40px 40px;` (use inline style for CSS variable reference since Tailwind can't generate radial-gradient utilities)
    - Render 3 sample RoadmapNode cards at absolute positions to demonstrate theme tokens. Use hardcoded positions (e.g., top: 100px left: 200px, top: 100px left: 500px, top: 250px left: 350px)
    - SVG connector placeholder: absolute, full size, pointer-events-none, z-index 1. One sample path with `stroke: var(--rv-line-connector)` stroke-width 1.5

    **StatusBar.tsx** (per UI-SPEC StatusBar section):
    - Height 32px, bg-rv-bg-statusbar, top border border-rv-border
    - Padding 0 14px, 11px text-rv-text-tertiary, z-index 100, user-select none
    - Three flex sections (left/center/right) with gap 10px
    - Left: green connection dot (7x7 rounded-full, use bg-rv-status-completed for the dot color), "Connected" text
    - Center: "sample-roadmap.json" filename
    - Right: "42 nodes" count, 12x12 icons text-rv-text-tertiary

    **ConfigPanel.tsx** (per UI-SPEC ConfigPanel section):
    - Floating: absolute, bottom-[60px] right-[16px], w-[260px]
    - bg-rv-bg-config, border border-rv-border, rounded-[10px], padding 16px
    - Shadow: style={{ boxShadow: "var(--rv-shadow-config)" }}
    - z-index 30, hidden by default (local useState toggle)
    - Toggle button: absolute bottom-[16px] right-[16px], 36x36 rounded-lg, bg-rv-bg-elevated border border-rv-border, shadow via var(--rv-shadow-node), text-rv-text-tertiary hover:bg-rv-bg-hover hover:text-rv-text-primary, 16x16 sliders icon
    - Title: "Canvas Options" 12px font-semibold text-rv-text-primary, 14x14 icon text-rv-text-tertiary
    - Node Corners toggle: label 11px font-semibold uppercase tracking-[0.04em] text-rv-text-tertiary; options "Rounded"/"Sharp" in bg-rv-bg-input border border-rv-border rounded-[6px]; active: bg-rv-accent-muted text-rv-accent
    - Connector toggle: "Curved"/"Straight" same pattern
    - Gap presets: "Compact"/"Default"/"Spacious" buttons, each flex-1, 11px font-semibold, rounded-[6px], bg-rv-bg-input border border-rv-border text-rv-text-tertiary; active: bg-rv-accent-muted text-rv-accent border-rv-accent-border

    **App.tsx** — Replace Plan 01's placeholder grid with actual component composition:
    ```tsx
    import { TopBar } from "./components/TopBar";
    import { Sidebar } from "./components/Sidebar";
    import { Canvas } from "./components/Canvas";
    import { SidePanel } from "./components/SidePanel";
    import { StatusBar } from "./components/StatusBar";

    export default function App() {
      return (
        <div id="app" className="grid h-screen w-screen bg-rv-bg-base" style={{
          gridTemplateRows: "auto 1fr auto",
          gridTemplateColumns: "auto 1fr auto",
          gridTemplateAreas: '"topbar topbar topbar" "sidebar canvas panel" "status status status"',
        }}>
          <TopBar />
          <Sidebar />
          <Canvas />
          <SidePanel />
          <StatusBar />
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>cd packages/desktop && grep -rE "(#[0-9a-fA-F]{3,8}|rgb\(|rgba\(|hsl\()" src/mainview/components/ src/mainview/hooks/ src/mainview/store/ --include="*.tsx" --include="*.ts" | grep -v "\.test\." | grep -v node_modules; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - packages/desktop/src/mainview/components/TopBar.tsx exists and exports `TopBar`
    - TopBar.tsx contains `role="toolbar"` and `aria-label="Main toolbar"`
    - TopBar.tsx contains `role="radiogroup"` (for theme switcher and layout toggle)
    - TopBar.tsx contains `setTheme("system")` call (System button per D-04)
    - TopBar.tsx contains `setTheme("dark")`, `setTheme("light")`, `setTheme("high-contrast")` calls
    - TopBar.tsx theme switcher has 4 buttons (dark, light, high-contrast, system)
    - TopBar.tsx theme switcher aria-checked compares against `preference` (not `resolvedTheme`)
    - packages/desktop/src/mainview/components/Sidebar.tsx exists and exports `Sidebar`
    - Sidebar.tsx contains `role="navigation"` and `aria-label="Sidebar navigation"`
    - Sidebar.tsx contains `w-[220px]` and `w-[48px]` (collapsed state)
    - Sidebar.tsx contains `aria-label="Collapse sidebar"`
    - packages/desktop/src/mainview/components/Canvas.tsx exists and exports `Canvas`
    - Canvas.tsx contains `radial-gradient` for dot grid
    - Canvas.tsx contains `rv-line-connector` for SVG connector
    - packages/desktop/src/mainview/components/StatusBar.tsx exists and exports `StatusBar`
    - packages/desktop/src/mainview/components/ConfigPanel.tsx exists and exports `ConfigPanel`
    - ConfigPanel.tsx contains "Canvas Options" and toggle options "Rounded", "Sharp", "Curved", "Straight"
    - packages/desktop/src/mainview/App.tsx imports all 5 components (TopBar, Sidebar, Canvas, SidePanel, StatusBar)
    - grep for hardcoded hex colors in components/ returns zero matches (excluding test files)
  </acceptance_criteria>
  <done>TopBar, Sidebar, Canvas, StatusBar, ConfigPanel all rendered in App grid. Theme switcher in TopBar has 4 buttons (dark, light, high-contrast, system per D-04) wired to setTheme. System button active state based on preference. Sidebar collapses. All components use only --rv-* tokens. Zero hardcoded colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RoadmapNode + SidePanel skeleton + component tests</name>
  <files>
    packages/desktop/src/mainview/components/RoadmapNode.tsx,
    packages/desktop/src/mainview/components/SidePanel.tsx,
    packages/desktop/tests/unit/ui/components.test.tsx
  </files>
  <read_first>
    packages/desktop/src/mainview/index.css,
    packages/desktop/src/mainview/components/Canvas.tsx,
    .planning/phases/01-visual-foundation-themes/01-UI-SPEC.md,
    .planning/phases/01-visual-foundation-themes/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: RoadmapNode renders title text
    - Test 2: RoadmapNode renders status badge with text label (not color-only — per PACK-06)
    - Test 3: RoadmapNode sets --node-stripe-color CSS variable matching status
    - Test 4: RoadmapNode sets --badge-color and --badge-bg CSS variables
    - Test 5: SidePanel renders field labels: STATUS, TYPE, CREATED, UPDATED, ID, NOTES
    - Test 6: SidePanel close button has aria-label="Close panel"
    - Test 7: SidePanel renders in closed state (width 0) by default
    - Test 8: Components contain zero hardcoded hex/rgb color values
  </behavior>
  <action>
    Per D-13, D-17, D-19:

    **RoadmapNode.tsx** (per UI-SPEC Node Card section):
    - Props: `{ title: string; status: "not-started" | "in-progress" | "completed" | "blocked" }`
    - STATUS_TOKEN_MAP constant mapping each status to its --rv-status-* color and --rv-status-*-bg tokens (per Research code example):
      ```typescript
      const STATUS_TOKEN_MAP = {
        "not-started": { color: "--rv-status-not-started", bg: "--rv-status-not-started-bg" },
        "in-progress": { color: "--rv-status-in-progress", bg: "--rv-status-in-progress-bg" },
        "completed": { color: "--rv-status-completed", bg: "--rv-status-completed-bg" },
        "blocked": { color: "--rv-status-blocked", bg: "--rv-status-blocked-bg" },
      } as const;
      ```
    - Root div: className `node relative` + min-w-[180px] max-w-[220px] rounded-[var(--node-radius,8px)] + border using `border-[length:var(--rv-border-width,1px)] border-[color:var(--rv-border)]` + bg using `bg-[var(--rv-bg-node)]` + shadow via style={{ boxShadow: "var(--rv-shadow-node)" }} + padding pl-4 pr-3 py-[10px] + select-none + transition for box-shadow/border-color/background 150ms + border-radius 200ms
    - CSS variables via inline style: `--node-stripe-color: var(${tokens.color})`, `--badge-color: var(${tokens.color})`, `--badge-bg: var(${tokens.bg})`
    - The 4px ::before stripe is handled by `.node::before` rule in index.css (already defined in Plan 01)
    - Hover: bg-[var(--rv-bg-node-hover)], shadow via var(--rv-shadow-node-hover) — use group/hover pattern or onMouseEnter state
    - Title: 13px font-semibold leading-[1.3] text-[var(--rv-text-primary)] mb-[6px] block
    - Badge pill: inline-flex items-center gap-[5px] px-2 py-[2px] rounded-[10px] text-[11px] font-semibold bg-[var(--badge-bg)] text-[var(--badge-color)]
    - Badge dot: 6x6 rounded-full bg-[var(--badge-color)] — status text label always visible (never color-only per PACK-06)
    - Status text: capitalize the status string, replace hyphens with spaces (e.g., "not-started" -> "Not Started")

    **SidePanel.tsx** (per UI-SPEC SidePanel section):
    - Props: `{ isOpen: boolean; onClose: () => void }`
    - Width: 340px when isOpen, 0 when closed. Transition: width 200ms ease-out
    - bg-rv-bg-panel, left border border-rv-border, shadow via style={{ boxShadow: "var(--rv-shadow-panel)" }}, z-index 50, overflow hidden
    - Grid area: `panel`
    - Header: padding 14px 16px, min-h-[52px], bottom border. Title "Node Details" 14px font-semibold text-rv-text-primary. Close button: 28x28 rounded-[6px] text-rv-text-tertiary hover:bg-rv-bg-hover hover:text-rv-text-primary, 14x14 X icon, aria-label="Close panel"
    - Body: padding 16px, overflow-y auto
    - Field sections with labels (11px font-semibold uppercase tracking-[0.05em] text-rv-text-tertiary mb-[6px]):
      - STATUS: status select display (padding 6px 10px, bg-rv-bg-input, border border-rv-border, rounded-[6px]) with 8x8 dot + label 12px text-rv-text-primary. Static "Not Started" for skeleton
      - TYPE: badge display (padding 3px 10px, 11px font-semibold, rounded-[4px], bg-rv-accent-muted text-rv-accent). Static "Task" for skeleton
      - CREATED / UPDATED: meta rows (key 12px text-rv-text-tertiary, value 12px text-rv-text-secondary). Static dates
      - ID: value with copy button (20x20 rounded-[4px], hover:bg-rv-bg-hover hover:text-rv-accent, 12x12 copy icon)
      - Divider: 1px bg-rv-border my-4
      - NOTES: 13px leading-[1.7] text-rv-text-secondary whitespace-pre-wrap. Static placeholder text with inline `code` styled bg-rv-bg-elevated text-rv-accent 11px rounded-[3px] font-mono

    **Component tests** in `tests/unit/ui/components.test.tsx`:
    - Use @testing-library/react render()
    - Mock the rpc module (vi.mock("../rpc")) to prevent Electroview import in test environment
    - RoadmapNode: render with title="Test Node" status="in-progress", assert title visible, badge text visible ("In Progress"), style contains --node-stripe-color
    - SidePanel: render closed (isOpen=false), verify width style is 0; render open (isOpen=true), verify close button has aria-label
    - CI hardcoded color check: after all component files written, verify grep returns 0 matches for hex colors in component source
  </action>
  <verify>
    <automated>cd packages/desktop && bunx vitest run tests/unit/ui/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/desktop/src/mainview/components/RoadmapNode.tsx exports `RoadmapNode`
    - RoadmapNode.tsx contains `STATUS_TOKEN_MAP` with entries for all 4 statuses
    - RoadmapNode.tsx contains `--node-stripe-color` in style prop
    - RoadmapNode.tsx contains status text label (not color-only)
    - RoadmapNode.tsx contains className `node relative` (for ::before stripe CSS)
    - packages/desktop/src/mainview/components/SidePanel.tsx exports `SidePanel`
    - SidePanel.tsx contains `aria-label="Close panel"`
    - SidePanel.tsx contains `w-[340px]` or width 340 logic
    - SidePanel.tsx contains field labels "STATUS", "TYPE", "CREATED", "UPDATED", "ID", "NOTES" (as rendered text, may be lowercase in source with CSS uppercase)
    - packages/desktop/tests/unit/ui/components.test.tsx exists
    - `bunx vitest run tests/unit/ui/` exits 0
    - grep for `#[0-9a-fA-F]{3,8}` in components/*.tsx (excluding test files) returns zero matches
  </acceptance_criteria>
  <done>RoadmapNode renders status stripe + badge pill with text labels. SidePanel skeleton shows all field sections. Component tests verify rendering and accessibility. Zero hardcoded colors in any component file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete app shell</name>
  <files>packages/desktop/src/mainview/App.tsx</files>
  <action>Human verifies the complete app shell with all 7 components (TopBar, Sidebar, Canvas with sample nodes, SidePanel, StatusBar, ConfigPanel) rendered in a CSS Grid layout. Theme switching between dark/light/high-contrast/system via top bar buttons (4 buttons per D-04). Sidebar collapse animation. All components use --rv-* token system exclusively.</action>
  <verify>
    1. Run `cd packages/desktop && bun run hmr` to start Vite dev server
    2. Open http://localhost:5173 in browser
    3. Verify dark theme renders by default — dark backgrounds, light text
    4. Click the sun icon in the top bar theme switcher — verify all surfaces switch to light theme colors
    5. Click the eye icon — verify high-contrast theme (black backgrounds, white borders, 2px border widths)
    6. Click the computer/monitor icon (System) — verify it follows OS preference (dark or light depending on Windows settings)
    7. Click the moon icon — back to dark theme
    8. Verify 4 theme buttons are present in the theme switcher radiogroup (moon, sun, eye, computer)
    9. Click the sidebar collapse button (left arrow icon) — verify sidebar animates from 220px to 48px, text labels hide
    10. Click the config toggle button (bottom-right of canvas) — verify floating config panel appears
    11. Verify 3 sample node cards are visible on the canvas with colored left stripes and status badge pills
    12. Click a node to open side panel (or verify side panel opens) — check field labels and close button
    13. Compare overall layout with `.planning/design/variant-c-merged.html` opened in a browser
    14. Open browser DevTools, inspect any component — confirm all colors come from CSS custom properties (no hardcoded hex)
  </verify>
  <done>User confirms app shell matches design reference, theme switching works across all four options (dark, light, high-contrast, system per D-04), and no hardcoded colors visible in DevTools inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component rendering | Static UI shell — no user data input in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Theme switcher | accept | Local UI control, no auth boundary; theme is cosmetic preference |
| T-01-04 | Denial of Service | CSS transitions | accept | 200ms transitions on local UI; no external input |
</threat_model>

<verification>
- All existing + new tests pass: `bunx vitest run` exits 0
- Zero hardcoded colors: `grep -rE "(#[0-9a-fA-F]{3,8}|rgb\(|rgba\(|hsl\()" packages/desktop/src/mainview/components/ --include="*.tsx" --include="*.ts"` returns 0 matches (excluding test files)
- All ARIA attributes present: grep for role="toolbar", role="navigation", role="radiogroup", aria-label in component files
- Theme switching changes all surfaces: visual verification at checkpoint
- Theme switcher has 4 buttons: dark, light, high-contrast, system (per D-04)
</verification>

<success_criteria>
- 7 components rendered in App.tsx grid layout matching variant-c-merged.html structure
- Theme switcher in TopBar has 4 buttons (dark, light, high-contrast, system per D-04) wired to useTheme store
- System button calls setTheme("system") and is active when preference === "system"
- Sidebar collapses/expands with animation
- Node cards show status stripe + badge pill with text labels
- SidePanel skeleton has correct field labels and ARIA attributes
- Zero hardcoded color values in any component file (CI grep clean)
- All component tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation-themes/01-02-SUMMARY.md`
</output>
